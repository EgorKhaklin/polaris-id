\* The tree as it is: exactly one setter of polaris.purge_in_progress, inside
\* uc_archive_purge, which writes the checkpoint in the same transaction.
\* check_formal_specs asserts that assumption holds in the SQL.
SPECIFICATION Spec
CONSTANTS
    Rows = {1, 2}
    Txs = {1, 2}
    RogueCarveOut = FALSE
    MaxRejects = 3
INVARIANT TypeOK
INVARIANT PurgeCoverage
INVARIANT NoRowVanishes
INVARIANT CarveOutIsPerTransaction
\* Two transactions with nothing left to do is a terminal state, not a seizure.
CHECK_DEADLOCK FALSE
