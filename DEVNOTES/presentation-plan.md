# The presentation sub-roadmap (P1.13 to P1.17)

**Reader:** the maintainer or agent opening a session to continue the
presentation pass authorized on 2026-09-02 (ROADMAP.md standing rule 8).
**Job:** the plan inside the plan: every ship the five rows decompose into,
its status, and the ordered changes, deletions and risks each ship carries,
so that one session can take one ship without re-deriving the audit. The
audit itself (177 findings across the README, `docs/`, the GitHub presence,
the demo site, the repository layout and the software's own surfaces) was
run by a workflow on 2026-09-02; this file is its synthesized plan, with the
critic's corrections applied as rulings. Update the status table with every
ship; the row is done when every ship under it is `[x]`.

## Status: closed (2026-09-04)

All twenty-nine ships are shipped, v9.194 through v9.225, and ROADMAP.md's
five rows are marked done. This file stays as the record of the pass: the
audit it synthesized, the rulings that resolved its contradictions, and the
ship-by-ship status below.

Three things were found during the pass that the audit had not:

- **The System Dashboard rendered blank from v9.211 to v9.220.** Deleting the
  boot overlay left the stagger rules that faded the panels in, setting them to
  `opacity: 0` with an animation whose keyframes were gone. Fixed at v9.220,
  and `check_css_animations_resolve` now fails on the class.
- **The Atlas legend named cyan zero-knowledge** while the map drew every
  clean verification cluster cyan, and a zero-knowledge verification is never
  plotted at all. Fixed at v9.214.
- **There was no `.dockerignore`**, so every image build shipped the git
  history, the site captures, the report and any local secret material to the
  daemon. Added at v9.225.

Deferred deliberately, each with its reason recorded in the ship that raised
it:

- ~~**A voice pass over the bodies in `docs/design/`.**~~ Done at v9.230 to
  v9.232, in three ships. It found more than voice: a second witness the
  documents said did not exist, a table shape that never existed, a rule
  catalogue describing apparatus removed at v9.55, a dependency manifest
  listing a library deleted at v9.221, and two stale deferrals for a Redis
  limiter that ships.
- **A second image format for the Atlas captures.** Measured at v9.218: WebP
  at quality 92 would cut them by three and a half times. Not applied, because
  the project has not decided to carry two formats for one picture.
- ~~**The remaining Phase 1 rows**, P1.11 (the retention engine) and P1.12 (the
  external penetration test), which are engineering and external work rather
  than presentation.~~ P1.11 shipped at v9.234 to v9.236, in three ships: the
  engine, the per-class purge, and the operator surface. It found two defects
  the pass had left standing, both in the archive chain rather than in
  presentation: the purge never verified an archive against its own manifest
  before deleting, and `scripts/polaris-test.sh` ran one suite while the
  preflight said it ran five. P1.12 stays open and is gated on funding and a
  firm.

## Rulings the critic forced, applied throughout

- QUICKSTART.md is deleted (shipped v9.197); its walkthrough and route table
  live in ARCHITECTURE-OVERVIEW.md. No later ship edits QUICKSTART.
- The table count is stated once, in one shape, and the check accepts exactly
  the two current numbers. It was "29 in `01_schema.sql`, 33 in a migrated
  deployment" through the pass; `RetentionPolicy` (v9.234) and the per-class
  purge columns made it 30 and 34. The ruling is the discipline, not the
  digits: one shape, restamped everywhere on the ship that changes it.
- SLH-DSA is ruled once, on every surface: registered in the algorithm table,
  no signer wired (shipped v9.194).
- The em-dash sweep is far larger than the plan assumed (docs 298, DEVNOTES
  176, meta 28, templates 173, static 102, deploy 38, polaris_checks 139 at
  v9.198); it is its own ship per surface, not a footnote.
- `docs/operator/SECURITY-CONTROLS.md` keeps its name; the rename's blast radius (checks,
  the landing page's absolute URL) exceeds its value.
- The DEVNOTES split (P1.16 ship 5) must be a mechanical move with every
  inbound reference repointed in the same commit; roughly eighty files cite
  DEVNOTES paths.
- The `ai-*` script renames (P1.16 ship 3) must update every caller, including
  `polaris-trace-drill.sh`, `polaris-chaos-test.sh`, the pre-commit config,
  CI, CLAUDE.md and CONTRIBUTING.md.
- Package metadata URLs (`polaris_cli/pyproject.toml`) point at
  `EgorKhaklin/polaris`; the repository is `EgorKhaklin/polaris-id`.
- The CLI is installed as `polaris-id` while every document says `polaris`;
  P1.17 ship 4 decides once and applies it everywhere.
- Endpoint names, route paths, environment variables and check names are
  never renamed by a presentation ship; only what a reader sees changes.
- `.github/FUNDING.yml` stays. The owner set up GitHub Sponsors across the
  project repositories on purpose; sponsoring the project is not a bug
  bounty, and SECURITY.md's no-bounty statement is about payment for
  findings. `check_presentation_surface` (P1.14 ship 5) must not require its
  absence.

## Ruling: the images live in site/, not assets/ (v9.218)

The plan called for `assets/` to stay canonical with `.github/workflows/pages.yml`
copying the binaries into the Pages artifact. Rejected on use. That arrangement
leaves `site/index.html` with four broken images when it is opened from a clone,
which is the moment someone is editing the page and needs to see it. Keeping the
one copy in `site/` costs a slightly unusual path in the repository README and
removes a build step, a staging directory and a class of drift. `site/README.md`
states the rule so the next editor does not re-split them.

Measured while re-exporting, and not applied: at 1600 pixels the two paired
captures lose about a tenth of their bytes, because the encoder that produced
them is already efficient; quantised to 256 colours they lose half their bytes
but also the legend's purple and amber, which are the colours the image exists
to explain. WebP at quality 92 would cut them by three and a half times, which
is a format decision the project has not taken.

## Status

| Row | Ship | Status |
|---|---|---|
| P1.13 | 1. Count and object truth pass, with enforcement checks | [x] v9.194 |
| P1.13 | 2. The constitution | [x] v9.195 |
| P1.13 | 3. The readiness ledger and the roadmap inventory | [x] v9.196 |
| P1.13 | 4. The architecture document; QUICKSTART absorbed | [x] v9.197 |
| P1.13 | 5. The operator surface | [x] v9.199 |
| P1.13 | 6. The reference surface | [x] v9.198 |
| P1.13 | 7. The indexes and the voice gate (docs hub, index-coverage check, PRINCIPLES merged, NOTICE and CONVENTIONS, CONTRIBUTING dedupe, the em-dash sweep across docs/ DEVNOTES/ meta/ and the package READMEs, the widened hook) | [x] v9.200 |
| P1.14 | 1. Repository settings and the security policy | [x] v9.201 (settings flipped by the owner 2026-09-03: alerts, security updates, private reporting, secret scanning, push protection; Projects off) |
| P1.14 | 2. About, topics, the four-surface self-description | [x] v9.202 |
| P1.14 | 3. Community health files | [x] v9.203 (FUNDING.yml kept; see the ruling above) |
| P1.14 | 4. CONTRIBUTING in public voice; README above the fold | [x] v9.204 |
| P1.14 | 5. Release-note shape; `check_presentation_surface` | [x] v9.205 (also: Code of Conduct 3.0; the README Atlas capture re-taken from the running app; P1.15 ship 3 and P1.17 ship 6 re-capture after the chrome changes) |
| P1.15 | 1. The site truth pass | [x] v9.216 (SLH-DSA and WebAuthn v9.194; the globe and street captions v9.213 and v9.214; the launcher tile, the warrant-grade caption, the notional-data badges, the re-measured test counts and the narrowed gating lede v9.216) |
| P1.15 | 2. The site structure pass | [x] v9.217 (What Polaris is, What this is not with the ledger link, the four run paths, the Evaluate it row, masthead and skip link and landmarks, the UC-kicker and duplicate removals, the head, robots.txt and 404.html) |
| P1.15 | 3. Assets and tokens | [x] v9.218 (one copy in site/ rather than assets/ with a Pages copy step, see the ruling below; the logo re-exported 942 KB to 44 KB; explicit dimensions and fetch priority; site/tokens.css under the application's token names, pinned by check_site_tokens_match_app) |
| P1.15 | 4. The site gate | [x] v9.219 (polaris-link-check.sh reads href/src and blob URLs; pages.yml gains a verify job and loses its path filter; the strong lede restored. check_stated_counts already covered site/index.html) |
| P1.16 | 1. Deletions | [x] v9.221 (atlas-globe.js with its 395 KB of vendored d3/topojson and the four documents that described it; ai-bootstrap.sh, polaris-concurrency-harness.sh, polaris-doctor.sh and polaris-idempotency-test.sh, whose property became a CI assertion; nginx.conf.example; two duplicate LICENSE copies with the rule in NOTICE; .claude/settings.json untracked; the stray coverage artifact. polaris-authz-audit.sh and polaris_authz_audit.py were KEPT: they work and docs/RED-TEAM-SCOPE.md points an assessor at them, so they are renamed in ship 3 instead) |
| P1.16 | 2. Asset de-duplication | [x] v9.218 (landed with P1.15 ship 3; assets/ deleted, site/README.md replaces assets/README.md and lists every file with its consumer) |
| P1.16 | 3. Script rename and index | [x] v9.222 (seven renames including polaris-release-notes.sh and the authz pair the plan had marked for deletion; every caller in the same commit; scripts/README.md indexes forty scripts by reader; CONVENTIONS.md §3 rewritten to one rule) |
| P1.16 | 4. Missing READMEs (deploy/, polaris_checks/, site/) | [x] v9.223 (site/ at v9.218, scripts/ at v9.222, deploy/ and polaris_checks/ here; all eight registered in SYSTEM-MAP.md and docs/README.md) |
| P1.16 | 5. DEVNOTES split and merges | [x] v9.224 (22 documents into docs/design/ with an index; the redaction-proof and quantum-observer duplicates resolved; headers given a reader and stripped of mission identifiers; DEVNOTES/ kept for the four internal notes. The voice pass over the moved bodies followed at v9.230 to v9.232) |
| P1.16 | 6. The map's coverage check and housekeeping | [x] v9.225 (check_system_map_covers_the_tree with its detection test; .dockerignore added, which the plan had not caught; the paper's status decided as a source-hash stamp plus check_paper_pdf_is_current rather than a LaTeX toolchain in CI; 00_load_all.sql's banner, prefixes and prerequisite corrected; .gitignore rewritten and CONTRIBUTING gains the clean command; docs/operator/SECURITY.md renamed to SECURITY-CONTROLS.md; CONVENTIONS section 1 corrected) |
| P1.17 | 1. The demo and launcher gates | [x] v9.206 |
| P1.17 | 2. The identifier and jargon sweep in templates | [x] v9.207 (also the template em-dash sweep, which P1.13 ship 7 left to this row) |
| P1.17 | 3. The messages | [x] v9.208 |
| P1.17 | 4. The CLI | [x] v9.209 (the `polaris` versus `polaris-id` name is decided: `polaris-id`, applied everywhere) |
| P1.17 | 5. Observability | [x] v9.210 (the metrics ACL now ships in both edges and is exercised in the caddy-edge job) |
| P1.17 | 6. The chrome and the seed | [x] v9.211 |
| P1.17 | 7. The Atlas captures and the readout scrim | [x] v9.213 (all three images re-taken at this version; the subject frame moved to the ordinary seed so the gold path is legible; the site's 3D-buildings and reticle captions corrected, which is part of P1.15 ship 1 landed early) |

Each ship follows the standing discipline in CLAUDE.md: edit, check, bump,
CHANGELOG, gate READY, CI green, release. A ship that touches a runbook or
the software is reviewed by a read-only factual lens before it commits.

---

# P1.13 (docs): Repair the facts first, then cut the archaeology: every human-facing document gets one named reader, one job, and numbers that a check recomputes.

## Target shape
About thirty documents, each opening with its reader and its job. MISSION.md is the constitution only: purpose, vocation, C1-C10 with SQL object names that resolve, the freeze line preserved verbatim with a dated status note recording that its clauses fired, permanent non-goals, amendment rule. docs/PRODUCTION-READINESS.md is inverted to open-items-first and points at ROADMAP for deployment-scale gaps. docs/README.md is a routing hub over four complete sub-indexes. docs/ARCHITECTURE-OVERVIEW.md ends at the architecture. The operator set has one owner per subject: DR.md owns recovery, ENCRYPTION-AT-REST.md owns at-rest, WEBAUTHN-ROLLOUT.md owns operator MFA, OPERATIONS.md owns day-2 and links out. The reference set describes the running code. No document body carries a version number, an internal ticket ID, or an arc/wave/Sanctum token. Every stated count is measured at the shipping version and pinned by a check. Voice is declarative present tense, no em-dashes, no cosmic framing, no defensiveness.

## Ordered changes
1. Land the enforcement before the edits. Extend polaris_checks/checks.py:1933 check_table_count_matches_doc with a hyphenated `(\d+)-table\b` alternative and add docs/reference/DATA-MODEL.md and CLAUDE.md to its guarded list; add a new check_stated_counts that imports len(CHECKS) from polaris_checks.checks (104, not a `^def check_` grep), counts top-level job keys in .github/workflows/ci.yml (14), and counts @app.route decorators in polaris_web/app.py, then asserts every occurrence of each in README.md, ROADMAP.md, docs/reference/SYSTEM-MAP.md and site/index.html. Follow the all-occurrences pattern and the fail-closed-on-no-count behaviour of check_table_count_matches_doc exactly, and pair each new check with a detection test on a broken fixture in polaris_checks/test_checks.py.
2. Re-measure the README evidence table by running the suites, then rewrite README.md:145-152 with one stamp at v9.193 covering all four rows: product tests, crypto witnesses, 104 invariant checks, 14 CI jobs (ci.yml). Strip the count from README.md:33, :56, :81, :115 so those read `the invariant layer`; fix ROADMAP.md:34 and README.md:87/:94 diagram counts by deleting them from the diagram entirely. Replace the six-behaviour CI paragraph with a compact list covering all 14 job families, and one line naming dr-drill.yml, sbom.yml and pages.yml with their real triggers. Preserve the sentence about jobs carrying a comment naming the failure they prevent, and the three-command verification block beneath it.
3. Correct the C1-C10 enforcement objects against the SQL before consolidating anything. In MISSION.md fix `disclosure_consistency` to `chk_disclosure_token_consistency` (polaris_sql/01_schema.sql:377), `reject_update_delete()` to `reject_audit_modification()` (polaris_sql/06_triggers.sql:85), attribute uq_one_active_per_person to polaris_sql/02_indexes.sql:19, `secure_headers()` to `apply_security_headers` (polaris_web/security.py:1158), and replace the C6 `enforce_zk_typing` trigger, which exists nowhere, with the real mechanism. Then delete the false trigger claims at docs/ARCHITECTURE-OVERVIEW.md:22, :53, :252 and the three-layer trigger paragraph at docs/operator/PRIVACY.md:85-94, and drop `under lock` from docs/story/PRINCIPLES.md:24. Keep README.md's `Enforced by` column, which is the most accurate table in the repo. Add check_c1c10_objects_resolve asserting every SQL object name cited in a C1-C10 enforcement claim in MISSION.md, README.md, ARCHITECTURE-OVERVIEW.md, PRINCIPLES.md and PRIVACY.md exists in polaris_sql/.
4. Fix the remaining count and link facts in one sweep: docs/ARCHITECTURE-OVERVIEW.md:109 to `Key tables (partial list)`; docs/reference/DATA-MODEL.md:3 to state 29 in 01_schema.sql plus the three migration-added tables (OperatorWebauthnCredential, OperatorSession, AuditAccessLog) explicitly, add sections for IndividualErasureEvent, ZkVerificationNonce, AuditAccessLog and OperatorWebauthnCredential, and regenerate its lines 5-19 summary from the section headings; docs/operator/README.md:24 to the proven RPO <= 5 min / RTO <= 4 h with DR-DRILLS.md as the ledger; docs/operator/DEPLOYMENT.md:115, docs/operator/SECURITY-CONTROLS.md:438-443 and docs/operator/INSTALL.md:125-136 lose their four contradictory test counts and gain the documented runner from CLAUDE.md:53; docs/operator/KUBERNETES.md's eight hardcoded 9.186 image tags become a VERSION shell variable read from polaris_web/__version__.py.
5. Add the additive governance note to MISSION.md immediately after the freeze-line heading, stating two already-recorded facts with links: the abandonment clause fired (docs/THESIS.md:3-4, the v9.40 terminus passed with no external cold read, the strong claim is retired as inconclusive) and the external trigger the section requires occurred on 2026-08-31 (ROADMAP.md:8-15, CHANGELOG.md v9.158), so the active arc is national deployment and the constitution is carried as a hard gate rather than softened. Alter no existing sentence in that section.
6. Cut MISSION.md lines 321-589: the v1 and v2 done-lists, the Arc D/E/F/G retirement narrative, and the Arc B phase log whose deferred items shipped. Before deleting, relocate the M2-1..M2-12 roster to a durable home (a CHANGELOG entry for this ship, or DEVNOTES) because grep -c "M2-" CHANGELOG.md is 0 while roughly thirty files still cite those labels; replace the bare M2-* identifiers surviving at MISSION.md:36 and :95 with plain names; remove the two memory/deferred_items.md citations and the meta/arc-b-production.md citation, none of which resolve; repair docs/README.md:46 and docs/SEED_DATA.md:162, which point at the done-list. Record the move in the CHANGELOG so the annotation's own promise that nothing was deleted stays true. Also remove the SCS-230 framing from MISSION.md:121-125 and docs/ARCHITECTURE-OVERVIEW.md:31-35, keeping provenance in exactly one place (NOTICE).
7. Invert docs/PRODUCTION-READINESS.md: status line, then the eight operator/legal/organizational decisions, then a stamped line stating the 39 enumerated engineering gaps are closed as of v9.193 with a CHANGELOG pointer, then a mandatory pointer to ROADMAP.md:41-58 for the deployment-scale gaps this ledger never tracked. Harvest the operator-gated caveats currently buried inside closed bullets (HSM/KMS custody, non-root Caddy edge, one postgres replica, offsite repo and schedule, Alertmanager backend, pager URL) into the decision table rather than deleting them. Compress the 250 lines of per-ship narrative into a claim-to-pinning-check table so the audit trail from prose to executable gate survives. Delete the 49/45/10 sentence and the uncitable v9.101 assessment reference at line 303 rather than restating either. Fix the closing rule, which currently implies production-ready arrives when the boxes are checked, and they all are.
8. Regenerate the ROADMAP.md "Where we are" inventory at v9.193 or replace it with a pointer: correct 77 to 104 and twelve to seventeen, remove the five debts the P0 table marks closed, cover the stale Do-not-have entries (HSM/KMS custody closed by P1.2, SBOM and provenance closed by P0.5/P0.6), and amend the P0 exit-gate sentence at ROADMAP.md:102 and :85 which reference the paragraph being rewritten. Collapse every `[x]` row's definition-of-done cell to item, version stamp, and pinning-check name, preserving in-row any clause that is a deferral or an open follow-up rather than a description of finished work (P0.6's image-signing deferral, P1.2's epoch-anchor scope note, P0.7's sibling-path witness follow-up).
9. Rewrite docs/ARCHITECTURE-OVERVIEW.md §IX around the four paths that exist, each as a Markdown link so polaris-link-check.sh covers it: docs/operator/INSTALL.md (macOS evaluation), DEPLOYMENT.md (single-host compose), LINUX-SERVER.md (scripted Linux under systemd), KUBERNETES.md (Helm reference profile). State the limits in the repo's own words from docs/operator/KUBERNETES.md:119-121 rather than inventing a recreation semantics the chart does not have. Convert the backticked docs/operator/DR.md and docs/design/threat-model.md references in the same section to Markdown links. Delete §XI and §XII. Strip every R*/M2*/vX.Y tag from §IV, §V and Further reading, and correct the stale line counts and the D3/topojson claim to MapLibre with its CARTO basemap CSP allowance.
10. Rewrite docs/RED-TEAM-SCOPE.md in the operator's voice for the commissioning firm: engagement type, threat actors, in-scope surfaces regenerated against the current tree (Caddy PQ edge, PKCS#11 and KMS custody, operator session registry, per-role network policy, agency quotas, pgBackRest and the DR path, Helm profile), out-of-scope with the volumetric-DoS carve-out restated, deliverables, disclosure timeline. Delete the agent-to-operator framing at lines 3-4, 11-22, 205-216 and the closing italic, but keep the five post-engagement commitments re-voiced as maintainer commitments (including the check_* regression-coverage item) and keep the plain statement that no engagement has been commissioned. Re-stamp the review date and re-stamp the matching (v9.23) pointer at docs/ARCHITECTURE-OVERVIEW.md:370. Drop the unstamped `All 29 tables` and the unstamped cost estimate.
11. Consolidate the operator set. Delete the OPERATIONS.md PITR recipe and the stale RPO/RTO paragraph at :703-711, replacing both with a pointer to DR.md section 1 and moving the managed-Postgres caveat into DR.md. Replace the four false entries in the OPERATIONS.md closing "does NOT cover" list with live cross-links, keeping multi-region and SOC 2 as genuinely uncovered and dropping the Arc B Phase N vocabulary. Delete the Recommended alerts and Metrics worth watching tables in favour of pointers to deploy/observability/polaris-alerts.yml, RUNBOOKS.md and SLOS.md. Fix the dead SECRETS.md "Health-check contract" pointer at :60-62 to docs/reference/API.md and replace the duplicated payload at :991-1018 with a pointer that keeps the Recommended alerts field names grounded. Regenerate the TOC against the 17 H2 sections. Move the pre-commit section to CONTRIBUTING.md.
12. Renumber docs/operator/SECRETS.md monotonically, move the WebAuthn enrollment and recovery runbook into WEBAUTHN-ROLLOUT.md (carrying §7.5 disabling-MFA and the POLARIS_WEBAUTHN_RP_NAME and relying-party-ID knobs, which are documented nowhere else), and repoint the five inbound section-number references (OPERATIONS.md:371, LINUX-SERVER.md:101, DR.md:372, :619, :621) at named anchors. Reconcile the three mutually inconsistent solo-admin recovery accounts against scripts/polaris-recover-admin.sh, which takes the mnemonic on stdin via --recovery-code, and correct WEBAUTHN-ROLLOUT.md:280's reference to a /auth/recovery route that does not exist.
13. Lead DR.md with the targets table and move the S3/pgBackRest configuration into its own section; delete the v9.126 honest-status blockquote and the ≤1-min RPO prose at :40-41 and :117 that FAILOVER.md:146 copied. Merge the OPERATIONS.md LUKS/TDE/fscrypt commands into ENCRYPTION-AT-REST.md section 6. Cut DEPLOYMENT.md to a router plus its unique environment-variable table, deleting the retirement paragraph, the duplicated credentials, and the Operational table whose backup and health rows contradict the shipped path. Rewrite docs/operator/SECURITY-CONTROLS.md as present-tense posture with the F-01..F-14 engagement narrative moved to a dated appendix and every "PDF §9.x" replaced by the named problem.
14. Fix the reference set: docs/reference/API.md gains the custody component and the /api/health/live and /api/health/ready subsections with a version placeholder rather than a literal, corrects the rate-limit table to 10 logins and one shared 60/60s per-IP write bucket while deleting the fabricated GET /api/atlas/* row and the R8-2 backlog sentence, drops the two docs/BACKLOG.md references and the false pre-commit completeness claim, documents the six missing /api/* routes, and removes POST /tokens/new and POST /tokens/<id>/edit which are not routes. Add check_api_routes_documented scoped to /api/* failing in both directions. Retitle docs/reference/SCALING.md around the 10-million-event measurement. Delete the internal-vocabulary entries from GLOSSARY.md. Replace the versioning-marker convention at docs/reference/README.md:44 with the P1.13 rule. Regenerate SYSTEM-MAP.md's tree, CI job list, and both docs lists from the filesystem.
15. Rebuild docs/README.md as a hub: every top-level docs/*.md listed with PRODUCTION-READINESS.md first and named as the bound on claims, plus one row per subdirectory delegating to its own index. Delete the false scripts-grep rationale, the "Added v8.59" and (v8.2x) archaeology, the M2-* enumeration, the agent-semantic-memory framing, and the Re-evaluation triggers section. Add DR-DRILLS.md and WEBAUTHN-ROLLOUT.md rows to docs/operator/README.md and drop the "(~1700 lines)" descriptor. Link docs/README.md from the root README, which currently never mentions it. Add check_docs_index_coverage asserting every docs/**/*.md is linked from the README of its own directory, wired into scripts/polaris-link-check.sh, which today validates that links resolve but cannot see an omission.
16. Merge docs/story/PRINCIPLES.md: fold the substitutability argument and the nine audit-of-record instances into MISSION.md or ARCHITECTURE-OVERVIEW.md, delete docs/story/, and repoint README, CONVENTIONS §14 and docs/README.md. Delete docs/QUICKSTART.md, moving the constraint-refusal SQL walkthrough into ARCHITECTURE-OVERVIEW.md (adding activated_date to the third block so it raises the uq_one_active_per_person error it advertises rather than the state-machine error) and the UI route table into ARCHITECTURE-OVERVIEW.md, not API.md. Fix ARCHITECTURE-OVERVIEW.md:7-8, the only live inbound link.
17. Close the voice pass: rewrite NOTICE's About section to two factual sentences and drop the CBDC paragraph, the SCS-230 paragraph and the undefined "post-v2 strategic moment framing" from the attribution clause; reconcile docs/CONVENTIONS.md §7/§9/§11 against the real ship procedure and delete the journal spec and the archive/ row; deduplicate CONTRIBUTING.md's three copies of the constitutional refusal and fix its count-to-four-name-two line; restamp SECURITY.md and CONTRIBUTING.md; remove the Arc/Wave/Sanctum/Position tokens from every operator and reference title and body; convert roughly 150 em-dashes across docs/, DEVNOTES/, meta/, site/ and the package READMEs; then widen .pre-commit-config.yaml's em-dash-block-new hook from five root files to the full human-facing set with CHANGELOG.md exempt, and restate its comment as a project standard.

## Deletions
- docs/QUICKSTART.md: README.md never links it, docs/README.md never lists it, and grep across polaris_checks/, scripts/ and site/ finds nothing; the only live inbound reference is docs/ARCHITECTURE-OVERVIEW.md:7-8, repaired in the same commit, and CHANGELOG.md:2084,2094 are history that stays.
- MISSION.md lines 321-589 (v1 and v2 done-lists, Arc D/E/F/G retirement narrative, Arc B phase log): no file links a MISSION.md anchor into that region; the only deep links anywhere are landing.html:115,118 to #vocation and #the-hard-constraints-do-not-violate, both outside the cut. polaris_checks/checks.py references MISSION.md only in comments (lines 10, 204, 211).
- docs/ARCHITECTURE-OVERVIEW.md lines 327-356 (§XI Steady state, §XII Where the project stands): grep for "§XI", "§XII" and "Where the project stands" across the repo returns only the two headings themselves; the range contains no links, no numbers, and no invariant, and check_table_count_matches_doc matches "29 tables" at line 100, far outside it.
- docs/operator/OPERATIONS.md lines 991-1019 (duplicated health contract), 1257-1338 (PITR recipe), 1020-1040 (Recommended alerts and Metrics worth watching), 1658-1700 (pre-commit hooks): grep finds zero inbound links to any of them; the only other PITR mentions are DR.md:121, 256, 467, and the alert set is owned by deploy/observability/polaris-alerts.yml with runbooks in RUNBOOKS.md.
- docs/story/ (PRINCIPLES.md) after merge: inbound references are README.md, docs/README.md:17, docs/reference/README.md and docs/CONVENTIONS.md §14 only, all repointed in the same commit; no check, test or script reads it.
- docs/PRODUCTION-READINESS.md lines 11-14 (the unreproducible 49/45/10 counts) and line 303 (the uncitable v9.101 assessment reference): find -iname "*assess*" returns nothing and the phrase appears only in this file and CHANGELOG.md, so the cited source is absent from the tree.

## Risks
- polaris_checks/checks.py:1933 check_table_count_matches_doc fails closed when a guarded doc states no count and requires every stated count to equal 29. Removing the table count from README.md or docs/ARCHITECTURE-OVERVIEW.md entirely breaks it, and writing 32 into either breaks it. The DATA-MODEL.md correction must therefore keep the 29-in-01_schema.sql figure and state the migration delta separately rather than replacing the denominator.
- Checks that read document text and will fail on a careless cut: check_dr_drill_scheduled (checks.py:3778) asserts DR.md points at the drill and the ledger; check_secrets_sealed (checks.py:3061) requires POLARIS_SECRETS_BACKEND and rotate-wrapping in SECRETS.md, both in section 8; check_session_origin_hardening (checks.py:3533) requires POLARIS_WEBAUTHN_ATTESTATION in WEBAUTHN-ROLLOUT.md; check_abuse_controls (checks.py:3674) pins the literal string AgencyQuota; the provenance check (checks.py:2724) requires the literal `gh attestation verify` in SECURITY.md; check_thesis_terminus_honest (checks.py:203-241) reads docs/THESIS.md and polaris_web/__version__.py and cites MISSION.md's freeze line as its stated authority.
- scripts/polaris-link-check.sh matches only Markdown the Markdown link form and quoted ./-relative code paths; backticked bare paths are invisible to it, which is how the dead `docs/DEPLOYMENT.md` reference survived at ARCHITECTURE-OVERVIEW.md:291. Every reference converted to a Markdown link newly enters the checked set, so run the checker after each conversion rather than at the end. It also skips the root CHANGELOG.md by path and never walks journal/.
- MISSION.md:67-76 declares that editing the freeze line to soften a condition or remove the abandonment clause breaks the constitutional contract and should be treated as a fork, and MISSION.md:19-21 states no further amendments are pre-authorized. The status note must be additive: keep the heading, the six done-conditions, the (a)/(b)/(c) work classes, the abandonment clause text and the tamper paragraph exactly as they stand, and keep the literal strings v9.40 and "abandonment clause" in the file.
- polaris_web/templates/landing.html:115 and :118 deep-link MISSION.md#the-hard-constraints-do-not-violate and MISSION.md#vocation. Both heading strings must survive the cut byte for byte or the public landing page silently breaks.
- Numbers must come from commands actually run. If a row of the README evidence table cannot be re-measured in the pass, leave that row's old value with its old stamp rather than guessing; a fresh stamp over an unmeasured number is a worse honesty failure than a stale one. Do not import the 49/45/10 counts, the "171 SQL self-tests" figure, or the unsourced 626 test total into any surface.
- docs/operator/DR-DRILLS.md is machine-appended by scripts/polaris-dr-drill.sh --record and committed monthly by .github/workflows/dr-drill.yml, which ci.yml deliberately ignores by path. It is a ledger, not a runbook: do not restyle it, do not count it as a runbook, and do not hand-edit rows.
- The C1-C10 consolidation must repair before it removes. MISSION.md's "Where enforced" column currently names five objects that do not exist under those names, so promoting it as normative without grep-verifying each against polaris_sql/ and polaris_web/ would canonicalize the errors and delete README.md:46 and :48, which are the two correct statements in the repo.

## Ship split
- Ship 1, the count and object truth pass: land check_stated_counts, the widened check_table_count_matches_doc and check_c1c10_objects_resolve with their detection tests, then re-measure and correct every stated count in README.md, ROADMAP.md, SYSTEM-MAP.md, DATA-MODEL.md, ARCHITECTURE-OVERVIEW.md and CLAUDE.md, and repair the C1-C10 enforcement objects plus the false C2 trigger claims. Nothing is deleted in this ship, so CI proves the enforcement before any text moves.
- Ship 2, the constitution: the additive MISSION.md governance status note, then the 269-line archaeology cut with the M2 roster relocated, the three dangling citations removed, and the SCS-230 framing moved to NOTICE.
- Ship 3, the ledger and the plan: invert docs/PRODUCTION-READINESS.md with the caveats harvested and the ROADMAP pointer added; regenerate ROADMAP.md's inventory and collapse its shipped rows, amending the two exit-gate sentences that reference the rewritten paragraph.
- Ship 4, the architecture document: §IX rewritten around the four real paths as Markdown links, §XI and §XII deleted, identifier and version tags stripped, line counts and the MapLibre claim corrected, and the QUICKSTART assets absorbed.
- Ship 5, the operator surface: OPERATIONS.md deletions and pointers plus its TOC, DR.md led by targets, SECRETS.md renumbered with WebAuthn relocated and five inbound references repointed, DEPLOYMENT.md cut to a router, encryption-at-rest merged, SECURITY.md re-voiced, RED-TEAM-SCOPE.md rewritten, and the Arc/Wave/Sanctum sweep across docs/operator/.
- Ship 6, the reference surface: API.md health contract, rate limits, dead references and the six missing routes with check_api_routes_documented; DATA-MODEL.md's four missing tables; SCALING.md retitled; GLOSSARY.md trimmed; reference/README.md's versioning convention replaced; SYSTEM-MAP.md regenerated.
- Ship 7, the indexes and the voice gate: docs/README.md rebuilt as a hub with check_docs_index_coverage, docs/operator/README.md completed, PRINCIPLES.md merged and docs/story/ removed, QUICKSTART.md deleted, NOTICE and CONVENTIONS and CONTRIBUTING and SECURITY.md restamped and deduplicated, the em-dash sweep, and the widened pre-commit hook landing last so it passes on arrival.

# P1.14 (GitHub): Make the front door say what the honesty ledger says, turn on the security features the prose implies, and give a reader the community files they expect.

## Target shape
A reader arriving at github.com/EgorKhaklin/polaris-id meets one sentence that says what Polaris is and that it is educational with notional data, topics that place it in the identity and post-quantum space rather than next to Flask tutorials, a README whose first screen carries a status badge and no defensiveness, a Security tab with a working Report-a-vulnerability button, secret scanning and push protection on, a SECURITY.md that routes reports privately and states the dependency merge policy, a CONTRIBUTING.md in public voice that keeps its AI-assistance disclosure and its C1-C10 refusal, issue and PR templates that ask for what CONTRIBUTING says it needs, a code of conduct, a citation file, no sponsor button, and releases titled by what changed for the reader.

## Ordered changes
1. Owner action in GitHub settings, in this order and before any document claims them: enable Dependabot alerts (GET /vulnerability-alerts currently 404s, which is why security updates cannot be enabled), then Dependabot security updates, then private vulnerability reporting, then secret scanning, then push protection. Disable the Projects tab, which renders empty.
2. Set the About to the project's own canonical sentence from CLAUDE.md:10-12 rather than minting a third variant: a working reference implementation of a post-quantum, zero-knowledge, duress-aware national identity-token system, educational, notional data only. Align README.md:9 and site/index.html:137 on `identity-token system` so all four surfaces match. Prune topics flask, postgresql, mfa, rust; add reference-implementation, fips-204, ml-dsa, slsa; keep the set under twelve.
3. Rewrite SECURITY.md: name the GitHub private-reporting path as primary with PolarisID@protonmail.com as fallback, keep the do-not-file-a-public-issue line, replace the plaintext `admin / Admin@123!` credential at :73-75 with a description plus a pointer to the quickstart, retitle the empty Hall of fame to Credit with the policy stated in one sentence, add the two-sentence Dependabot merge policy so a reviewer can reconcile 36 closed Dependabot PRs with the commit history, add the batch note that majors are deliberate roadmap work, restamp, strip the (v8.95+) and "shipped v9.13" archaeology, and reconcile the acknowledgement window against polaris_web/app.py:2331, which emits 72 hours in the RFC 9116 body while SECURITY.md:35 says five business days. Keep the literal `gh attestation verify` string.
4. Rewrite CONTRIBUTING.md in public voice: drop the VANTA persona, the "holy shit, that's done" slogan, the `VANTA's standing instructions` framing, the DEVNOTES/style.md and CLAUDE.md gotcha-ordinal pointers (inline the one-sentence CSP rule instead), and the `Agent: Claude (Anthropic)` credit line. Keep the plain-prose AI-assistance disclosure, the C1-C10 refusal stated once, the six-item merge-readiness list, the check_* plus detection-test requirement, the will-not-accept list, and the SECURITY.md pointer. Add one line stating what scripts/polaris-test.sh and scripts/polaris-preflight.sh do rather than renaming them here; the rename belongs to P1.16.
5. Add the missing community files: CODE_OF_CONDUCT.md (Contributor Covenant 2.1, enforcement contact PolarisID@protonmail.com, linked from CONTRIBUTING.md's opening); .github/ISSUE_TEMPLATE/config.yml with blank_issues_enabled false and a contact link routing security reports to private reporting; a bug-report form capturing component, the version from /api/health, and reproduction; a change-proposal form capturing the C1-C10 or vocation alignment CONTRIBUTING asks for; PULL_REQUEST_TEMPLATE.md with motivation, change, blast radius and the test-discipline checklist; CITATION.cff with the version pinned to polaris_web/__version__.py, referenced from the README row that offers the PDF.
6. Delete .github/FUNDING.yml. Trim the 32-line internal decision-record header in .github/dependabot.yml to the five lines a reader needs, move the per-version history to CHANGELOG.md, drop "the maintaining agent", and correct the line-1 and line-5 claims that the file configures advisory-driven PRs when it configures weekly version updates and the repository setting supplies advisories.
7. Fix the README above the fold: replace "It is not a slide deck" with the plain capability statement, add a status badge reading reference implementation, not production, linking docs/PRODUCTION-READINESS.md, add one SBOM and provenance pointer near the badge row, keep the row to four badges, and remove the counts from the architecture diagram so the single stamped evidence table owns them.
8. Adopt a fixed release-body shape generated from the CHANGELOG block: one-paragraph summary, Breaking changes (or None), Upgrade, Verify this release with the `gh attestation verify` command and the artifact names, Details linking the CHANGELOG entry. Retitle v9.188 through v9.192 by what changed for the reader, keeping the roadmap row as a trailing reference line and dropping the "Next opener" and check-count tally from the public body.
9. Add check_presentation_surface to polaris_checks, file-based only: CODE_OF_CONDUCT.md, CITATION.cff, .github/ISSUE_TEMPLATE/config.yml and PULL_REQUEST_TEMPLATE.md exist; .github/FUNDING.yml does not; SECURITY.md and CONTRIBUTING.md stamps are within one minor of polaris_web/__version__.py; SECURITY.md names the private-reporting path and retains the attestation command. Do not add a GitHub API probe to the merge gate: both the private-vulnerability-reporting and security_and_analysis endpoints require admin permission the default GITHUB_TOKEN lacks, so pinning them would need a standing admin PAT as a CI secret on a repository that advertises SLSA provenance, and would fail on fork PRs. If a live probe is wanted, put it in a scheduled maintainer-only workflow that does not gate merges.

## Deletions
- .github/FUNDING.yml: one line, `github: EgorKhaklin`, referenced by nothing in the tree; it renders a Sponsor button that contradicts SECURITY.md:144-148 ("There is no formal bug-bounty payout. Polaris is a reference implementation, not a deployed service.").
- SECURITY.md's plaintext `admin / Admin@123!` at :73-75: the same credentials appear at README.md:176-179, which stays as the single quickstart surface; no check or test reads the SECURITY.md copy.
- .github/dependabot.yml lines 2-32 and 55-57 (the internal decision-record comment block): comments only, read by nothing; the per-version history is already in CHANGELOG.md.
- README.md's architecture-diagram counts at :87 and :94: the diagram is a shape, not a measurement, and the same figures live in the one stamped evidence table; check_table_count_matches_doc requires at least one table count to remain somewhere in README.md, which the evidence table supplies.

## Risks
- polaris_checks/checks.py:2724-2728 raises a FAIL for `provenance` if the literal string `gh attestation verify` is absent from SECURITY.md. Any rewrite of the supply-chain section must keep it verbatim.
- check_table_count_matches_doc fails closed when README.md states no schema-table count. Stripping counts from the diagram is safe only while the evidence table still states 29.
- .pre-commit-config.yaml:48,60 gates new em-dashes on CONTRIBUTING.md, README.md, SECURITY.md is not covered today but will be after the P1.13 widening, so the replacement prose must use colons or sentence breaks.
- Do not enable a security setting after a document claims it. Writing the private-reporting path into SECURITY.md before the setting is on converts a configuration gap into a fresh overclaim, pointing readers at a button that does not exist.
- Do not assert anywhere in prose that secret scanning or push protection is enabled as a standing claim. Repository settings can be flipped silently and no in-repo check can observe them; an unstamped claim becomes a liability the moment it drifts.
- The About string lives in GitHub settings, outside version control, so no diff, review or check can pin it. Describe it as a manual mirror of CLAUDE.md:10-12, not as drift-proof, or add an explicit `gh api ... --jq .description` comparison in a non-gating scheduled workflow.
- README.md:220 and ROADMAP.md:36 say twelve operator runbooks against seventeen documents plus a ledger in docs/operator/. Fix in P1.13; do not restate a count here.

## Ship split
- Ship 1, repository settings and the security policy: the five GitHub settings in dependency order, Projects disabled, then SECURITY.md rewritten to match what is now true, with the dependabot.yml header corrected in the same commit so the file and the setting stop contradicting each other.
- Ship 2, the About sentence, the topics, and the four-surface alignment of the canonical self-description across About, README.md:9, site/index.html:137 and CLAUDE.md:11.
- Ship 3, the community health files: CODE_OF_CONDUCT.md, the two issue forms plus config.yml, PULL_REQUEST_TEMPLATE.md, CITATION.cff, and the FUNDING.yml deletion.
- Ship 4, CONTRIBUTING.md rewritten in public voice and the README above-the-fold pass (badges, status badge, the slide-deck sentence, diagram counts).
- Ship 5, the release-note shape plus retitled recent releases, and check_presentation_surface with its detection test.

# P1.15 (site): Turn the Pages site from a poster into a front door: state what it is and is not, correct every crypto claim to what the code enforces, and put the numbers behind a gate before claiming they are gated.

## Target shape
One page with a masthead nav and section anchors, opening with what Polaris is (the credential-consolidation premise the page currently assumes), then the Atlas showcase with a legible notional-data statement beside the figures, then a What this is not section at h2 weight carrying the ledger's own status line and the eight operator decisions with a link to docs/PRODUCTION-READINESS.md, then the guarantees, the threat cards without internal use-case numbers, an evidence block whose every number is recomputed from the tree at a single stamp, a cryptography section whose ML-DSA, SLH-DSA, WebAuthn and TLS claims match the code and the posture document, four real deployment paths, and an Evaluate it link row reaching the security policy, the PQC posture, the runbooks, the API reference and the roadmap. Images de-duplicated against assets/, sized, and served with explicit dimensions. Design tokens shared with polaris.css rather than forked.

## Ordered changes
1. Correct the numbers and drop the claim the page cannot yet support. Fix site/index.html:145 to 29 schema tables and the route count to the measured value, :146, :229 and :240 to 104 invariant checks, :232's `1 / double-click to launch` tile deleted, and re-measure the 571 and 50 tiles; restamp :234 at v9.193. Rewrite the :224 lede from "Every claim on this page is backed by a gate that fails if the claim stops being true" to what is true today, and restore the stronger sentence only in the ship that lands the gate.
2. Fix the four crypto and privacy claims against the code. Line 251's SLH-DSA hedge cell becomes an algorithm-registry statement ending "Today only the ML-DSA-65 signer is implemented" (polaris_web/pqc_signing.py:125 hardcodes _ALG_NAME and verify rejects every other label), or the cell is deleted; line 246's "No hardcoded algorithms anywhere" narrows to the storage claim it can support. Line 259's WebAuthn cell becomes a second factor after the password, gating admin once a credential is enrolled and refusing login after the deadline, operators optional, auditors read-only and exempt, ML-DSA-65 offered first (webauthn_auth.py:250-252, :494, :526-532). Line 249's ML-DSA cell names the production default and the labelled SHA3-256 development placeholder. Line 156's globe caption states the mechanism (excluded from every plotted set in SQL) rather than "they carry no location, by construction", which polaris_sql/01_schema.sql:353-357 and 04_data.sql:205 contradict. Line 165 drops "warrant-grade" for the control that exists: restricted to admin and auditor, audit-logged on every use.
3. Add the missing scope statement at h2 weight. A What this is not section above Run it, carrying docs/PRODUCTION-READINESS.md:3 verbatim in substance, the eight operator-decision row names from :314-321 without a numeral, and the first link from this page to the ledger. Fold the 12px paragraph at :276-279 into it and keep the hero scope line at :138 as the above-the-fold marker. Rename the :271 subhead from Production to a phrasing that names the compose profile without asserting readiness, changing README.md:184 in the same pass so the two front doors do not diverge.
4. Add a Notional data statement to all three figures as page text, not baked into the image: the figcaptions at :156, :161, :165 gain "Notional data. Every name, agency, and coordinate in these captures is invented." in --amber at caption size, the three alt texts gain it, and a CSS corner badge is positioned over each <figure> so the label survives cropping. Do not re-bake banners into the PNGs and do not expect a resolution change to make the in-image 10px stamp legible: width:100% fixes the rendered glyph at about 4.6px regardless of source pixels.
5. Add the structure the page lacks: a What Polaris is section carrying the credential-consolidation premise so the :191 hard-parts lede has an antecedent; an Evaluate it link row before the footer reaching PRODUCTION-READINESS.md, PQC-POSTURE.md, MISSION.md, SYSTEM-MAP.md, API.md, DATA-MODEL.md, the security and privacy postures, the operator runbooks and ROADMAP.md; ids on all six sections plus a sticky anchor nav styled as the app masthead; a skip link and <main>; and aria-labelledby on every section and figure, mirroring polaris_web/templates/landing.html.
6. Restructure Run it around the four shipped paths: evaluate locally, Linux server under systemd pointing at deploy/linux/install.sh and LINUX-SERVER.md, Kubernetes reference profile pointing at deploy/helm/polaris and KUBERNETES.md, and single-host compose, each carrying its stated limit from the roadmap. Delete the UC-6 through UC-12 kickers from the threat cards, keeping the human titles. Delete the "which is the point" anecdote at :236, the duplicate duress claim at :258 in favour of the accurate one at :198, and one of the two Latin motto occurrences at :139 and :285.
7. Fix the head and the assets: canonical, theme-color #050a12, color-scheme dark, og:site_name, og:image:alt and dimensions, twitter:card, plus site/robots.txt and site/404.html. Add explicit width and height to every img and fetchpriority=high to the logo. Export the logo under 40KB and the atlas captures at 1600px. Then de-duplicate: keep one canonical copy of each of the four PNGs and favicon.svg in assets/ (all four are byte-identical across assets/ and site/ today), and have pages.yml copy them into the upload artifact so a screenshot refresh cannot leave a stale twin. Extract the shared design tokens into site/tokens.css using polaris.css's canonical names.
8. Land the gate last, then restore the stronger lede. Extend check_stated_counts from P1.13 to site/index.html so the schema-table, route and invariant-check figures are recomputed from the tree on every push; extend scripts/polaris-link-check.sh to resolve href and src in HTML, including repo-relative blob/main URLs stripped to tree paths, restricting any backtick-path widening to slash-bearing strings with known extensions; add a verify job to .github/workflows/pages.yml running the link check before upload, and widen its triggers beyond `site/**` so a schema or check change revalidates the page. Keep the count enforcement in polaris_checks rather than in pages.yml, because pages.yml's path filter makes it structurally blind to the commits that cause the drift.

## Deletions
- site/index.html:232, the `1 / double-click to launch` tile: a macOS convenience affordance sitting in the evidence grid beside CI counts; nothing links or tests it, and no gate can fail on it.
- site/index.html:251-252, the SLH-DSA hedge cell: no SLH-DSA signer or verifier exists: polaris_web/pqc_signing.py:125 hardcodes ML-DSA-65 and verify_signature rejects every other label; grep for SLH across polaris_web/*.py returns only registry-seed assertions in tests; docs/reference/PQC-POSTURE.md's gap table has no SLH-DSA row. The .cells grid is auto-fit and reflows to five without layout damage.
- site/index.html:236's "which is the point" clause, :258's duplicate duress claim, and the UC-* kickers at :195: none is referenced anywhere; grep across polaris_checks/, scripts/ and .github/ finds no reader of site/ content at all, and pages.yml only uploads the directory.
- site/atlas-globe.png, site/atlas-street.png, site/atlas-subject.png, site/polaris_logo_clean.png, site/favicon.svg as committed second copies: shasum confirms all four PNGs are byte-identical to their assets/ twins (aaf15d07, 73193b52, 270a3389, 25b3e028) and favicon.svg matches polaris_web/static/. Only site/index.html references them, by relative filename, so the Pages build supplies them instead.

## Risks
- .github/workflows/pages.yml triggers only on paths ['site/**', '.github/workflows/pages.yml'], so any gate placed there never fires on the commits that cause the drift (a new table in polaris_sql/01_schema.sql, a new check in polaris_checks/checks.py, a new job in ci.yml). The count enforcement must live in polaris_checks, which ci.yml runs on every push; pages.yml gets the link check only.
- pages.yml uploads path: site/ only. De-duplicating the images into assets/ breaks the deployed site unless the copy step lands in the same commit, and any relative href to docs/ would 404 on the published site, so all outbound links must be absolute github.com/.../blob/main/ URLs matching the page's existing convention at :142.
- Nothing in polaris_checks, scripts/ or .github/ reads site/ content today (grep for `site/` across polaris_checks/*.py and the gate scripts returns nothing), which is precisely why the page drifted 36 versions. There is no existing check to break, and no test-pinned markup on this surface.
- Numbers must be re-measured, not restamped. The 571 product tests and 50 crypto witnesses on the site carry the same v9.157 stamp as README.md's table; both must move together in the same measurement, or the two front doors diverge again in the opposite direction.
- Do not restore the "every claim on this page is backed by a gate" sentence until the gate exists. Shipping the corrected numbers under the old lede leaves the page's central epistemic claim false for another release.
- Do not describe /api/health/ready as the Kubernetes readiness probe if the deployment paths are described: deploy/helm/polaris/templates/app.yaml:81-90 wires startup, readiness and liveness all to /api/health/live, and Caddyfile:50 uses health_uri /api/health/live.
- Design-token drift is invisible after the rename: the site redeclares polaris.css's tokens under different names (--dim for --ink-dim, --gold-b for --gold-bright), so a future redesign would desynchronize without a greppable signal. Use the canonical names in site/tokens.css or the shared-token check cannot be written.

## Ship split
- Ship 1, the truth pass: correct the six numbers and the stamp, delete the launcher tile, rewrite the five crypto and privacy claims (SLH-DSA, ML-DSA default, WebAuthn, warrant-grade, the globe caption), narrow the :224 lede to what holds, and add the notional-data statement to the three figures. No structural change, so it can ship immediately and independently of the gate.
- Ship 2, the structure pass: What Polaris is, What this is not with the ledger link, the Evaluate it link row, the four deployment paths, the UC-kicker and anecdote removals, section ids, nav, skip link, main, aria-labelledby, and the head and robots and 404 additions.
- Ship 3, the assets and tokens pass: logo and capture re-export, explicit dimensions, fetchpriority, de-duplication into assets/ with the pages.yml copy step, and site/tokens.css extracted from polaris.css's :root.
- Ship 4, the gate: check_stated_counts extended to site/index.html with its detection test, polaris-link-check.sh extended to HTML href and src, the pages.yml verify job and widened triggers, and the restored stronger lede now that it is true.

# P1.16 (repository): Delete what nothing calls, give every surviving top-level path a stated reader, and rename the scripts for what they do rather than who runs them.

## Target shape
A tree that explains itself on first open. Thirty-six scripts under one naming rule, indexed by scripts/README.md with a reader column and the caller that invokes each. No `ai-` prefix anywhere, so nothing in the operational surface reads as agent tooling. Every top-level directory carries a README naming its reader, including deploy/, polaris_checks/ and site/. The assessor-facing material currently filed as agent memory (threat model, concurrency, ZK soundness, audit-of-record, the ships/ design records) sits in the published docs set with an index entry; only genuinely internal working notes remain in a directory named for what they are. One copy of each binary asset, one copy of the license, one document per subject. docs/reference/SYSTEM-MAP.md is regenerated from git ls-files and pinned by a check so it cannot silently drift again.

## Ordered changes
1. Delete the six dead scripts and their dead references: scripts/ai-bootstrap.sh, scripts/polaris-authz-audit.sh, scripts/polaris_authz_audit.py, scripts/polaris-concurrency-harness.sh, scripts/polaris-idempotency-test.sh, scripts/polaris-doctor.sh. Before deleting the last two, decide whether the properties they prove (00_load_all idempotency, measured C3 behaviour under load) should become CI assertions; if so port them into .github/workflows/ci.yml in the same commit. The launcher's doctor subcommand stays and is the documented diagnostic at README.md:193.
2. Untrack .claude/settings.json (git rm --cached) and remove the working-tree file. Its PreToolUse hook invokes scripts/polaris-ai-done-hook.sh, which does not exist anywhere in the tree; CHANGELOG.md:4423 records that wrapper as deliberately deleted in the pass that cut AI-methodology scaffolding, and .gitignore:89 already declares .claude/ per-user. Do not re-add the hook script: that would reverse a documented decision. Do not edit the CHANGELOG entries.
3. Delete polaris_web/nginx.conf.example and rewrite polaris_web/README.md:100-101 and :446, its only two referrers, to point at the Caddy edge (Caddyfile, Dockerfile.caddy) and at docs/operator/DEPLOYMENT.md and LINUX-SERVER.md. docs/operator/DEPLOYMENT.md:48-51 records that the native nginx path was retired in v9.176 for bypassing container hardening, the pgbouncer and postgres TLS hops, pgBackRest and the secrets layout, so the committed sample is a live path to an insecure deployment.
4. Delete polaris_web/static/atlas-globe.js and the dead CSS rule blocks it left behind in polaris_web/static/polaris.css (.d3-globe-node, .globe-toolbar, .globe-toolbar-time, .atlas-toolbar, .atlas-stage, .atlas-container, plus .atlas-id-strip-secondary, .globe-hud-tr, .globe-hud-bl, .hud-row, .hud-value-primary, .live-indicator, .badge-live, .filter-chip, .health-sub, .notice-copy, .notice-light, .notice-time). Keep every dynamically composed selector (.pill-*, .auth-*, .ctx-*, .audit-*, .disclosure-cell-*, .agency-type-*, .flash-*) and correct the stale docstring at polaris_web/test_e2e_atlas.py:8 that still describes the page as a WebGL globe rendered by atlas-globe.js. Three documents describe the same dead file and the vocabulary it used: docs/reference/SCALING.md:110, :225 and :230 (the frontend section, and "reticle ornament with leader line + label", which the current renderer does not draw), docs/design/atlas-scaling.md:21, and polaris_web/README.md:195. They move to atlas-map.js and to marker, the word the interface uses since v9.211.
5. De-duplicate the binaries. Keep one canonical copy of the four PNGs and favicon.svg in assets/, have .github/workflows/pages.yml copy them into the Pages artifact, and rewrite assets/README.md so its file table lists every file present with its consumer, deleting the What this directory is NOT section. Delete polaris_zk/LICENSE and rely on the SPDX license field in polaris_zk/Cargo.toml; keep or delete polaris_cli/LICENSE with the reason stated once in NOTICE, so the asymmetry across the four packages becomes a rule instead of an accident.
6. Remove the empty journal/ and sanctum/ directories from the working tree (neither appears in git ls-files, so neither exists in a fresh clone) and clean up the references that survive them: the journal/ row at docs/CONVENTIONS.md:20 and its path spec at :153, the archive/ row at :22, the now-dead skip branch at scripts/polaris-link-check.sh:91-92, and the journal/ clause at .gitignore:154. Replace the dangling sanctum citation at meta/tla/c3-one-active-token.tla:24 with the reasoning stated inline, and either define Sanctum once in docs/reference/GLOSSARY.md or replace it everywhere with a plain phrase such as a recorded amendment decision.
7. Rename the surviving ai-* scripts by job, updating every caller in the same commit: polaris-coverage.sh to polaris-coverage.sh (7 referring files), polaris-link-check.sh to polaris-link-check.sh (10), polaris-test.sh to polaris-test.sh (12), polaris-preflight.sh to polaris-preflight.sh (5). Callers to update: .github/workflows/ci.yml:162 and :198, .pre-commit-config.yaml, .coveragerc, polaris_checks/checks.py and test_checks.py, polaris_web/test_app.py, polaris_web/__version__.py's documented bump procedure, README.md:160, CONTRIBUTING.md:62-74, CLAUDE.md:53-56, docs/ARCHITECTURE-OVERVIEW.md:275, and docs/CONVENTIONS.md:47-52 where the agent-versus-operator naming split is codified. Standardise the Python helpers on one separator (polaris_load_gen.py). Leave CHANGELOG.md history unrewritten and add a CHANGELOG entry mapping old names to new.
8. Add scripts/README.md: one table of script, one-line job, named reader (operator, CI, contributor), and the caller that invokes it. Add deploy/README.md naming the three substrates and which is supported versus reference, polaris_checks/README.md mapping C1-C10 to the check_* functions with the run command and the statement that it gates CI and pre-commit, and site/README.md stating that this is the Pages source published by pages.yml on push to main.
9. Split DEVNOTES/ by reader. Move the assessor-facing documents (threat-model.md, substrate.md, zk-soundness.md, two-witness-principle.md, audit-of-record.md, concurrency.md, rasp-rules.md, observability.md and the ships/ design records) into the published set under docs/reference/ or a new docs/design/, with an index entry and a stated reader; keep only genuinely internal working notes and rename the directory for what it holds. Merge the two redaction-proof documents into one (keep the longer meta/ version carrying the adversary model, delete meta/redaction-proof.md, strip the Introduced-v8.x and joint-review provenance) and merge the two quantum-observer documents into one that states plainly it is a reserved schema scaffold. Delete the v8.26 reorganization note from DEVNOTES/README.md, regenerate both of its tables from ls, and resolve or drop the unexplained Glass and Pentecost citations and the census-roll.json and treasury-roll.json references.
10. Fix the loader's own honesty in polaris_sql/00_load_all.sql without renaming files: move the ALL FILES LOADED banner from line 79-82 to after the final \i at line 95, correct its text (12_v7_constraints.sql and 13_substrate.sql emit assertions after 08_tests.sql's total), add the missing \echo before 12_v7_constraints.sql (the only silently sourced file, and the one whose absence the header says costs C-NEW-1..C-NEW-4), strip the ship IDs from the \echo strings, rewrite the (v8.32 maintenance note) header as a plain superuser prerequisite, and add three comment lines stating that the numeric prefixes are file identifiers and the \i sequence is authoritative, with the reason for each deviation. Defer any renumbering to its own ship: 09_grants.sql is deliberately sourced twice and scripts/polaris-migrate.sh:479-488 pins three filenames in an OBJECT_FILES array whose miss path is a silent skip.
11. Rewrite .gitignore without release numbers, decision-class labels or incident narrative, deleting the meta/brain-map/ and polaris_swarm/* rules for subsystems that no longer exist and the polaris-mycelium-wake.sh reference. Keep the one-line functional note that a pattern must not carry a trailing inline comment, without the history. Add a clean target removing .coverage*, .pytest_cache/, __pycache__/ and polaris_zk/target/, document it in CONTRIBUTING.md, and confirm the Docker build context excludes the same set: a stray host-named coverage artifact currently sits in polaris_cli/.
12. Regenerate docs/reference/SYSTEM-MAP.md's At a glance tree from git ls-files so every tracked top-level path appears with a one-line role, give deploy/ three sub-lines, name all four workflow files and the 14 ci.yml jobs in one compact list, correct the ROADMAP.md description from R-* to the P-series, and drop the .git genesis line and the portfolio front-page label. Soften or delete the completeness promise at lines 3-5 unless it will be generated. Add a check following the check_alert_runbooks pattern at polaris_checks/checks.py:1083 (missing and orphan both fail) asserting set equality between the map's top-level directory list and git ls-files, its operator and reference filename lists and the directories, and its ci.yml job ids and the workflow's job keys. Scope it to tracked paths so it does not trip on gitignored operator-local directories.
13. Rename docs/operator/SECURITY-CONTROLS.md to docs/operator/SECURITY-CONTROLS.md so root SECURITY.md is the only file with the GitHub-recognised name, add a one-line pointer between them, and list root SECURITY.md in docs/README.md's What lives elsewhere section. Settle on one maintainer identity across SECURITY.md:153, CONTRIBUTING.md:154 and docs/THESIS.md:5. Decide the paper's status: publish the 1.9 MB rendered PDF as a Release asset and remove it plus its .gitignore carve-out, or keep it and add a CI step that rebuilds it from the .tex and fails on divergence.

## Deletions
- scripts/ai-bootstrap.sh (12.3 KB), scripts/polaris-authz-audit.sh, scripts/polaris_authz_audit.py, scripts/polaris-concurrency-harness.sh, scripts/polaris-idempotency-test.sh, scripts/polaris-doctor.sh: grep -rl for each basename excluding .git, CHANGELOG.md and the file itself returns nothing at all, except polaris_authz_audit.py which is reachable only from the dead polaris-authz-audit.sh. polaris-doctor.sh's own header cites docs/operator/DR.md and polaris-oracle-runner.sh as callers; grep -n doctor docs/operator/DR.md returns nothing and polaris-oracle-runner.sh does not exist. None appears in .github/workflows/*.yml except through the blanket `for f in scripts/*.sh; do bash -n` syntax check.
- .claude/settings.json: grep -rn polaris-ai-done-hook excluding .git returns exactly one hit, CHANGELOG.md:4423, inside a Removed block; the hook target ls scripts/polaris-ai-done-hook.sh returns No such file and find for *done-hook* is empty repo-wide. git check-ignore --no-index confirms .gitignore:89 covers the path once untracked, so no .gitignore edit is needed. It is the first entry of git ls-files.
- polaris_web/nginx.conf.example: grep -rn nginx.conf.example excluding .git and CHANGELOG.md returns exactly two hits, polaris_web/README.md:100 and :446, both rewritten in the same commit.
- polaris_web/static/atlas-globe.js (1538 lines): polaris_web/templates/atlas.html:247-248 loads only vendor/maplibre-gl.js and atlas-map.js; no template, no route in app.py and no other JS names the file. The surviving `atlas-globe` string matches are the live DOM ids atlas-globe-data and atlas-globe-detail and a CSS class, which stay.
- site/*.png and site/favicon.svg as committed duplicates, polaris_zk/LICENSE, journal/, sanctum/: shasum proves the four PNGs byte-identical across assets/ and site/; git ls-files returns nothing under journal/ or sanctum/, so neither exists in a fresh clone; the Apache text is byte-identical (b3858b4c, 11,306 bytes) across /LICENSE, polaris_cli/LICENSE and polaris_zk/LICENSE.
- DEVNOTES/README.md's v8.26 reorganization note and rename table, meta/redaction-proof.md, docs/design/quantum-observer.md: the reorganization note is a 2026-05-11 migration receipt; the two merge targets are superseded by the longer meta/redaction-proof.md and by a single consolidated scaffold note, and docs/README.md routes readers to the meta/ copy already.

## Risks
- scripts/polaris-migrate.sh:479-488 pins 11_atlas.sql, 14_foresight_helpers.sql and 15_ateology-layer files by literal name in an OBJECT_FILES array whose loop responds to a missing path with `echo skip; continue`. A rename that misses that array does not error: it silently stops syncing those objects on every persistent-volume upgrade, the same silent-loss class the loader header warns about. This is why the SQL renumber is deferred out of this row.
- polaris_checks/checks.py:2014 asserts the literal string 11_atlas.sql, checks.py:106 and scripts/polaris_authz_audit.py:40 read literal polaris_sql paths, and polaris_checks/test_checks.py:279, :297, :391, :538, :558 carry fixtures keyed to those filenames. Any SQL rename must update all of them in one commit.
- The ai-* rename touches .github/workflows/ci.yml:162 and :198, .pre-commit-config.yaml, .coveragerc, polaris_web/test_app.py, polaris_web/__version__.py's documented bump procedure and the /done skill. It must be mechanical, in one commit, with CI green before merge; CHANGELOG.md's historical references stay as written, so a name-mapping entry is required or two naming worlds persist.
- polaris_checks/checks.py:1967 check_launcher_current asserts polaris_mac_launch.sh installs from requirements.txt and names polaris_checks, test_check_constraints, test_invariants_property, test_redaction_property and the ZK binary; check_launcher_refreshes_code asserts it re-applies the atlas function file. Neither reads a script name being renamed, but the launcher itself invokes the suites, so verify it after the rename.
- polaris_web/test_app.py:286 asserts atlas-fullbleed, atlas-id-strip and atlas-globe-data, and polaris_web/test_e2e_atlas.py:173 asserts the #atlas-globe-data JSON island. Deleting atlas-globe.js is safe; renaming those DOM ids is not.
- Deleting DEVNOTES documents or moving them changes roughly two dozen inbound links from docs/reference/API.md, docs/RED-TEAM-SCOPE.md:68 and :162, docs/operator/OPERATIONS.md, PRIVACY.md and SECRETS.md:374, plus docs/ARCHITECTURE-OVERVIEW.md's Further reading. Move with the links, and do not strip the meta/constraint-lattice.md, meta/structural-architecture.md and docs/design/threat-model.md citations from ARCHITECTURE-OVERVIEW: they are the traceability an assessor follows.
- scripts/polaris-link-check.sh SKIP_DIRS lists .claude and archive/, and it skips the root CHANGELOG.md by path and never walks journal/. Removing journal/ makes the skip branch dead but harmless; removing the archive/ skip is only safe once docs/CONVENTIONS.md:22 stops listing a directory that does not exist.
- docs/CONVENTIONS.md:26 states the rule never to rename a top-level directory casually, on the grounds that it touches the audit-of-record and thousands of cross-references. That rule as written forbids exactly this row's work; replace it with a rule that renames land together with the checks and cross-references they touch, rather than working around it.

## Ship split
- Ship 1, the deletions: six dead scripts, .claude/settings.json untracked, nginx.conf.example with its two referrers rewritten, atlas-globe.js with its dead CSS and the test_e2e_atlas.py docstring, polaris_zk/LICENSE, the empty journal/ and sanctum/ directories with their four surviving references, and the stray coverage artifact. Subtractive only, so CI proves nothing was load-bearing.
- Ship 2, the asset de-duplication: one canonical copy per binary in assets/, the pages.yml copy step, and assets/README.md rewritten to list every file with its consumer.
- Ship 3, the script rename and index: four renames with every caller updated in one commit, scripts/README.md, and docs/CONVENTIONS.md:47-52 rewritten from an agent-versus-operator split to a job-based rule.
- Ship 4, the missing READMEs: deploy/, polaris_checks/, site/, each naming its reader in the first sentence, added to SYSTEM-MAP.md and docs/README.md.
- Ship 5, the DEVNOTES split and merges: assessor-facing documents moved into the published set with their inbound links, the two redaction-proof documents merged, the two quantum-observer documents merged, DEVNOTES/README.md regenerated and its reorganization note deleted.
- Ship 6, the map and the housekeeping: SYSTEM-MAP.md regenerated with its tree-coverage check and detection test, .gitignore rewritten with the clean target, docs/CONVENTIONS.md §1 corrected, docs/operator/SECURITY-CONTROLS.md renamed to SECURITY-CONTROLS.md, and the paper's status decided.
- Deferred to its own ship, not this row: renumbering polaris_sql/ so prefix order equals load order. It cannot reach its stated goal while 09_grants.sql is deliberately sourced twice, and it touches two checks, five test fixtures, four scripts and two copy-paste operator recipes. The in-file loader honesty fix (banner position, missing echo, ship IDs, header) lands in Ship 6 at zero blast radius.

# P1.17 (software): Gate the demo and launcher surfaces on declared deployment state, strip every internal identifier from what an operator reads, and make the messages one voice.

## Target shape
An operator moving through the application meets one register: sentence-case declarative messages, menus named for tasks rather than requirement numbers, an Atlas whose provenance label tells the truth about the deployment it is running in, error pages that name what happened and carry the request id, and no page that cites a PDF the reader does not have. The public demo walkthrough and the macOS launcher beacon exist only where they belong, each behind its own explicit flag with a safe default. The CLI's help lists all twenty commands, its examples and exit codes, and a version. The log stream's event names are namespaced and inventoried where an operator will look. The stylesheet carries no dead rules and no boot-sequence theatre. Every test-pinned string that moves is repinned in the same commit.

## Ordered changes
1. Introduce two flags on separate axes, both derived from state that already exists, and inject them through the context processor at polaris_web/app.py:675 rather than reading env in templates. POLARIS_DEMO_MODE defaults to `not _PRODUCTION` (app.py:237) and refuses to enable when _PRODUCTION is true, so a production deployment cannot advertise synthetic data over real records and a checkout cannot silently lose its honest label. POLARIS_LAUNCHER_WATCH defaults off and is set by polaris_mac_launch.sh and the dev compose. Update docs/QUICKSTART.md:153 and :164 and any route table that lists /demo in the same change.
2. Gate the demo surface on POLARIS_DEMO_MODE: register /demo (app.py:1108) only when it is on, and render the landing CTA pair conditionally so Sign in becomes the single primary action with the demo CTA absent (landing.html:15-22 holds the only url_for('demo') in the repo, so wrapping it avoids a Jinja BuildError). Add a test that /demo returns 404 under production mode and update the now-stale comment at polaris_web/test_app.py:3606-3609.
3. Invert the Atlas provenance labels rather than deleting them. polaris_web/templates/atlas.html:221's classification strip, :15's OPERATIONAL id strip, :118's OP / POLARIS-LIVE collection readout and :111's LIVE chip all assert provenance and currently contradict each other on one screen. Drive all four from the same flag: non-production renders a synthetic-data label, production renders an operator-configured deployment label or nothing. Do not substitute a classification marking Polaris cannot know is true, and drop the em-dash. Update polaris_web/test_app.py:280 and :306 in the same commit and add a test exercising the production branch.
4. Gate the three launcher-support routes and the beacon on POLARIS_LAUNCHER_WATCH: /api/heartbeat (app.py:715), /api/quit (:727) and /api/since-heartbeat (:739), plus the unconditional heartbeat.js tag at base.html:144, which currently POSTs every ten seconds from every rendered page including the unauthenticated login page. Gate the rate-limit exemption at app.py:541-546 with them. Keep security.reject_cross_site applied and keep its four CrossSiteGuardTests plus the three HeartbeatTests running against a launcher-mode client, because those two routes are that guard's only coverage. Correct docs/reference/API.md:834-837, which documents POST /api/quit as authenticated and admin-only when it has no auth and no role check, and :825-828, which says /api/heartbeat returns a timestamp when it returns 204 empty. Prefer deleting /api/since-heartbeat outright: polaris_mac_launch.sh:895-897 reads the state files from disk and no caller of that route exists anywhere.
5. Strip internal identifiers from every operator-facing template, updating the pinned assertions in the same commit. base.html:37-52 SUBSTRATE menu loses its R10-2, R10-1 and R11-3 kickers and gains a task-shaped name; dashboard.html:46 and tokens_detail.html:180 lose the v2 Substrate headings and the R-number tile sublabels (test_app.py:6758, :6847, :6873, :6882 repointed); duress_queue.html:5 loses (R11-5 / M2-10) and its R1/R2/R3/R6 labels become the plain names already written beside them, with the closing DEVNOTES footnote replaced by the notification action an operator needs; epochs_list.html:99's Substrate-D closure item and exploration-Sanctum footnote are deleted; demo.html loses its eleven inline version citations and its R-numbers become mechanism names, keeping the C1/C2/C3/C7 labels, which are the one identifier scheme defined for the reader.
6. Remove the surveillance and film vocabulary. atlas.html:117-119's Collection / OP / POLARIS-LIVE stack is deleted (it never changes value); reticle becomes marker in the legend and the Node Console help; Node Console becomes Event detail; atlas-map.js:373's LIFECYCLE NODE and VERIFICATION NODE kickers follow. polaris.css:1964's Atlas god view comment and the .god-notice-feed and .god-notice class names become .atlas-feed and .atlas-feed-item with the .notice-* modifiers unchanged (two referrers: atlas.html:205 and atlas-map.js). Delete the 1.6-second post-login boot overlay at dashboard.html:98-115 and polaris.css section 19, plus the .scifi-reveal stagger and its --reveal-delay inline styles.
7. Replace every PDF §N and Appendix A citation with the in-repo document that carries the requirement or with nothing: uc8_revoke.html:12, uc6_migrate.html:8, uc9_initiate.html:8, individuals_enrollment.html:7 and :68, tokens_detail.html:43. Strip the paired M2-N / R11-N identifiers in the same pass. Rewrite the truncated Sanctum parenthetical at verifications_form.html:75 to the operator-relevant fact the rest of that comment already states well.
8. Fix the messages. Adopt one flash rule (complete declarative sentence, terminal punctuation, no colon-prefixed status words) and apply it to every flash() call site in app.py, taking db_error_to_message at :433-463 as the target register. Add the missing .flash-warning rule and a warning glyph in base.html:127, so the WebAuthn lockout countdown at app.py:815 stops rendering as a neutral notice. Change flash-dismiss.js so only success flashes auto-dismiss and error and warning flashes carry a dismiss control, because the current 4.5-second timer erases the only report of a failed write. Fix error.html: derive the headline from `code` in the template rather than threading a status_word no handler passes, delete the unreachable 503 branch (both 503 sites return tuples, so an errorhandler would never fire), address the operator directly, and render observability.get_request_id() rather than asking for a browser timestamp. Rewrite login-form.js's three OPERATOR ID / CREDENTIAL strings in sentence case matching the form's own labels.
9. Fix the SQL console and the CLI. sql_console.html:6 drops the hardcoded polaris_test database name (POLARIS_DB_NAME's dev default at app.py:314) and the hardcoded polaris_app role name, keeping the accurate SELECT/WITH, no-DDL and cap sentences. In polaris_cli/polaris.py, regenerate the module docstring's Commands block from HANDLERS so the two cannot drift (six of twenty commands are currently missing, including revoke and both halves of the recovery ceremony), delete the reference to the report, move the Examples and Exit codes blocks into the epilog where RawDescriptionHelpFormatter will render them, and add --version reading polaris_web/__version__.py.
10. Rewrite polaris_web/observability.py's docstring for an operator: what it emits, the four metric definitions, the log record shape, and the one operational instruction. Move the Sanctum, meta-swarm and BIG MISSION Tier 8 lineage to the CHANGELOG. Namespace the event names and separate state announcements from events (auth.failure, auth.quota_refused, db.error, duress.signal, boot.session_policy, boot.tracing_enabled) and publish the inventory in the same docstring. Reconcile the two contradictory /api/metrics and /metrics docstrings into one statement, written once in deploy/observability/README.md's access-control section generalized to both paths and referenced from app.py:2298-2301, :2352-2357 and :2359-2365: any surface carrying duress_events_total is operator-monitoring-only behind an ACL, and the control is access to the surface, not suppression of the metric. Delete the expose-no-per-user-data clause as a rationale for public reachability, and add the same one-line ACL note to docs/design/observability.md:34-35 and docs/operator/SLOS.md:18.
11. Finish the chrome. Delete the Schema v1.0 span and the Latin motto from base.html:138, replacing the version if wanted with POLARIS_VERSION through the context processor. Replace every hand-written ?v= cache-bust stamp across base.html and the leaf templates with one value derived from POLARIS_VERSION, and delete the polaris-scifi.css history sentence at base.html:8-10. Renumber polaris.css's section markers to match physical order, regenerate its header index, and delete the three orphaned subsection comments carried from the merged skin file. Render the _pager.html Cursor mode label as page size alone and repoint test_app.py:5917, :5927 and :5944 at the cursor= query parameter in the Next link. Replace the seed individual Egor Khaklin at polaris_sql/04_data.sql:47 with a synthetic name in the same shape as the other three, updating test_app.py:754 and polaris_cli/test_cli.py.
12. Rewrite polaris_web/templates/landing.html:173-177: the heading becomes Deploying the reference stack and the lede states that the deployment path ships in the repo and is scripted end to end, that Polaris is a reference implementation and is not yet a system you can run with real national-identity data, and links docs/PRODUCTION-READINESS.md beside the existing OPERATIONS.md and SECRETS.md links. Delete Arc B (May 2026). Amend the ML-DSA-65 cell at :124-129 to name the gate: real signing under the production default, a labelled deterministic placeholder otherwise, with a pointer to docs/reference/PQC-POSTURE.md added to the Read more list at :199-206. Keep the SHA3-256 Merkle sentence at :135-136 unchanged, which is exactly the label PQC-POSTURE.md:51-55 calls honest. Change class names and ids nowhere: polaris.css:1614, 1636, 1745 and 1751 key off landing-deploy, landing-section-lede, landing-codeblock and landing-deploy-after.

## Deletions
- polaris_web/templates/dashboard.html:98-115 and polaris.css section 19 (the post-login boot overlay, .scifi-reveal stagger and --reveal-delay inline styles): no test asserts on the overlay text; it contradicts polaris.css:9's own stated contract of an intelligence-report aesthetic with no decorative flourishes, and the masthead user strip already shows username and role permanently.
- polaris_web/templates/atlas.html:117-119 (the Collection / OP / POLARIS-LIVE stack): the field never changes value and no test references it; test_app.py's Atlas assertions are :280 on the id strip, :286 on three class names, and :306 on OPERATIONAL ATLAS, all elsewhere.
- polaris_web/templates/epochs_list.html:99 list item 4 and its exploration-Sanctum footnote: tests pin ZK Epochs and Leaves for epoch #1, not the explainer; the first three list items are accurate operator content and stay.
- polaris_web/templates/error.html:29-35 (the code == 503 branch): no @app.errorhandler(503) and no abort(503) exist; the only 503s (app.py:2223 health roll-up, app.py:2375 metrics fallback) return status tuples directly, which Flask error handlers never intercept, and /api/health is machine-consumed with tests asserting status_code in (200, 503).
- polaris_web/templates/base.html:138's Schema v1.0 span and Latin motto, and the polaris-scifi.css history sentence at :8-10: the version literal has no referent (the application is at 9.193 and the schema is migration-versioned), the motto is decoration on the chrome of the duress queue and the warrant-audit results, and the merged skin file no longer exists.
- polaris_web/app.py:739-745, GET /api/since-heartbeat: its docstring reads `Anyone can call this; no auth`, and grep across all shell, YAML and launcher files shows no caller anywhere; polaris_mac_launch.sh:895-897 reads $STATE_DIR/heartbeat and $STATE_DIR/quit from the filesystem directly.

## Risks
- The test-pinned markup contract, enumerated, all of which must be repinned in the same commit as the string that moves: test_app.py:280 `<div class="atlas-id-strip">OPERATIONAL</div>`; :286 atlas-fullbleed, atlas-id-strip, atlas-globe-data; :306 OPERATIONAL ATLAS; :3276, :3287 and :6766 Duress Signals; :5917 and :5927 assertIn('Cursor mode') with :5944 assertNotIn, so both branches need a new pin; :6758 v2 Substrate; :6847 v2 Substrate State; :6873 and :6882 SUBSTRATE; :4145 the '/sql': ('admin','auditor') role map; test_e2e_atlas.py:173 the #atlas-globe-data JSON island.
- polaris_web/test_app.py PROTECTED_PATHS omits /demo and no test asserts it returns 200, so gating it breaks nothing today, but the comment at :3606-3609 stating that /demo is intentionally public becomes false and must move with the change.
- security.reject_cross_site (security.py:1135) is applied to exactly /api/heartbeat and /api/quit and nowhere else. Deleting rather than gating those two routes silently retires that guard and its only coverage; gate them and keep CrossSiteGuardTests (test_app.py:6927-6952) and HeartbeatTests (:389-405) running against a launcher-mode client.
- Do not add a demo flag whose default is ambiguous. Default-on means a production deploy that forgets to set it labels real records as synthetic; default-off means the demo silently drops its only honest label. Deriving from POLARIS_ENV, which app.py:254, :281 and :300 already make mandatory in production with fatal startup refusals, fails safe in both directions.
- polaris_checks/checks.py:3674 pins the literal string AgencyQuota and checks.py:1126-1141 keys on polaris_duress_events_total and PolarisDuressEvent, not on the prose being rewritten. The observability docstring rewrite is safe; renaming the metric is not.
- polaris.css:1614, 1636, 1745 and 1751 key off the landing-deploy, landing-section-lede, landing-codeblock and landing-deploy-after class names, and landing.html:115 and :118 deep-link MISSION.md anchors. The landing rewrite is text-only.
- Neither polaris_web/Caddyfile nor deploy/helm/polaris/templates/configmap-caddy.yaml actually restricts /metrics or /api/metrics, so the ACL the docstrings will describe is documentation-only. State it as an operator requirement, not as an enforced control, and log the deployment-artifact gap separately rather than fixing it inside a copy pass.
- polaris_web/templates/demo.html carries the claim that every procedure shown is real and every constraint named is enforced at the database layer. Gating the page does not make that true; the version citations and R-numbers must still be stripped, and the claim must survive review against what the walkthrough actually exercises.

## Ship split
- Ship 1, the gates: POLARIS_DEMO_MODE and POLARIS_LAUNCHER_WATCH through the context processor, /demo and the landing CTA gated, the four Atlas provenance labels driven from one flag, the three launcher routes and the beacon gated with their rate-limit exemption, /api/since-heartbeat deleted, docs/reference/API.md's two false statements corrected, and every affected assertion plus a production-branch test landed together.
- Ship 2, the identifier and jargon sweep: base.html, dashboard.html, tokens_detail.html, duress_queue.html, epochs_list.html, demo.html, the five PDF-citation templates and verifications_form.html, with the six pinned assertions repointed in the same commit.
- Ship 3, the messages: the flash register pass across app.py, .flash-warning plus its glyph, flash-dismiss.js scoped to success, error.html's derived headline and request id with the dead 503 branch removed, login-form.js in sentence case, and sql_console.html's hardcoded database and role names.
- Ship 4, the CLI: docstring regenerated from HANDLERS, the report reference deleted, Examples and Exit codes moved into the epilog, and --version added.
- Ship 5, observability: the module docstring rewritten for an operator, event names namespaced with the inventory published, and the /api/metrics versus /metrics contradiction resolved into one statement with the two adjacent operator surfaces carrying the same line.
- Ship 6, the chrome and the seed: footer, cache-bust from POLARIS_VERSION, the boot overlay and Collection stack deleted, god-view class rename, polaris.css renumbered with its dead selectors removed, the pager label, the seed individual's name, and the landing.html deployment and ML-DSA cells.

# Cross-cutting
- One voice, defined mechanically so it can be enforced rather than asserted. Declarative present tense; each document opens by naming its reader (operator, integrator, assessor, contributor) and its one job; no em-dashes; no version number, ticket ID, arc, wave, Sanctum or Position token in any document body; no cosmic framing, no Latin, no defensiveness against imagined criticism; numbers only where a number is load-bearing, and then measured and stamped. Enforcement: widen .pre-commit-config.yaml's em-dash-block-new hook from the five root files it covers today (README, MISSION, ROADMAP, CLAUDE, CONTRIBUTING, which are already clean at 0-1 occurrences) to docs/**, DEVNOTES/**, meta/**, site/** and the package READMEs, after the one-time sweep of roughly 150 occurrences, with CHANGELOG.md exempt as the append-only record. Restate the hook's comment as a project standard rather than a named person's instruction. Voice does not extend to CHANGELOG.md history: past entries are the audit of record and are never rewritten.
- Identifier vocabulary: exactly one scheme survives on human-facing surfaces, and it is C1-C10. Those are defined in MISSION.md, explained on the landing page, and carried in the schema; they earn their place. Every other scheme goes: R10-*, R11-*, M2-*, UC-* (as a visible kicker), Arc B/D/E/F/G, Wave N, Position B, Tier 8, Substrate-D, and every `PDF §N` and `Appendix A` citation to a document the reader does not have. Where an identifier was the only cross-reference into a design record, replace it with the record's path, not with nothing. Before deleting the M2-* roster from MISSION.md, land it somewhere durable: grep -c "M2-" CHANGELOG.md is 0, so the claim that the CHANGELOG holds it is false, while roughly thirty files still cite the labels.
- Script naming: rename by job, not by who runs it, and delete what nothing calls. The `ai-` prefix is the single loudest signal that a national identity system's verification tooling was written for an agent, and it is factually misleading, since polaris-coverage.sh and polaris-link-check.sh are CI gates and a committed pre-commit hook. Two of the seven ai-* files are dead and get deleted; the surviving four become polaris-coverage.sh, polaris-link-check.sh, polaris-test.sh and polaris-preflight.sh, with all 34 referring files updated in one mechanical commit and a CHANGELOG name-mapping entry so an operator holding an old runbook can follow it. docs/CONVENTIONS.md:47-52, which codifies the agent-versus-operator split as policy, is rewritten to a job-based rule in the same ship. Python helpers standardise on one separator. Deferred deliberately: renumbering polaris_sql/, which cannot reach prefix-order-equals-load-order while 09_grants.sql is intentionally sourced twice, and whose miss path in scripts/polaris-migrate.sh's OBJECT_FILES array is a silent skip rather than an error.
- Docs index structure: a hub with delegation, not a flat mirror, and coverage rather than uniqueness as the enforced property. docs/README.md lists all eight top-level docs/*.md completely, with PRODUCTION-READINESS.md first and named as the bound on every claim, then one row per subdirectory pointing at its own index. docs/operator/README.md and docs/reference/README.md are already the strongest indexes in the tree and are completed rather than absorbed: re-listing their 22 rows at the top level is exactly the duplication that produced the current drift. The new check asserts that every docs/**/*.md is reachable from docs/README.md through the index graph, and that no index row names a file that does not exist; it does not demand a unique index entry, because SYSTEM-MAP.md and PRINCIPLES.md are deliberately cross-referenced and a uniqueness rule would force deleting working reading paths. It extends scripts/polaris-link-check.sh, which today verifies that links resolve but is structurally unable to see an omission, and equally unable to see a backticked bare path, which is how a blocker-severity dead link survived in a repo that runs link-check on every commit.
- Explicit demo mode: two flags on two axes, both derived from state that already exists, neither with an ambiguous default. POLARIS_DEMO_MODE defaults to `not _PRODUCTION` and refuses to enable when POLARIS_ENV=production, which app.py:254, :281 and :300 already make mandatory with fatal startup refusals. It gates the /demo route, the landing demo CTA, and the Atlas provenance labels, and the Atlas gate is an inversion rather than a deletion: the synthetic-data label must appear in exactly the deployments whose data is synthetic and disappear in exactly the ones whose data is not. POLARIS_LAUNCHER_WATCH is separate, defaults off, and is set by the macOS launcher and the dev compose; it gates the three heartbeat routes, their rate-limit exemption, and the ten-second beacon that today fires from every page including the unauthenticated login page. Do not fold the launcher into demo mode: they are different concerns with different safe defaults.
- Numbers and stamps: one measurement, one stamp, one place, and a check that recomputes it. Today the repository states its own invariant count five ways in README.md alone (102 twice, 77 three times) against 104 registered checks, its CI coverage as 7 against 14 jobs, its schema as 26, 28 and 29 across four documents, and its test totals four different ways across three operator documents, none within a factor of four of the code. The rule: a count appears once per surface, under a stamp naming the version it was measured at, and a check_stated_counts recomputes it from the tree (len(CHECKS) imported from the module, not a `^def check_` grep; ci.yml job keys; CREATE TABLE in 01_schema.sql; @app.route decorators) and fails on any occurrence that disagrees, across README.md, ROADMAP.md, SYSTEM-MAP.md and site/index.html together. Corollary that matters more than the rule: a fresh stamp over an unmeasured number is a worse honesty failure than a stale one, so any row that cannot be re-measured in the pass keeps its old value with its old stamp.
- Enforcement placement: gates live in polaris_checks, which ci.yml runs on every push, not in path-filtered workflows and not in CI steps holding privileged credentials. .github/workflows/pages.yml triggers only on site/** and would never fire on the commits that cause site drift, so it gets the link check and the count check goes to polaris_checks. The GitHub metadata checks (About string, topics, private vulnerability reporting) stay file-based or move to a non-gating scheduled workflow, because both relevant API endpoints require admin permission the default GITHUB_TOKEN lacks, and storing a standing admin PAT as a CI secret on a repository that advertises SLSA provenance trades a supply-chain risk for a documentation risk. Every new check ships with a detection test on a broken fixture, per the layer's own rule, and follows check_table_count_matches_doc's all-occurrences pattern, whose comments record two prior drift regressions (v9.89 README-versus-schema, v9.141 first-match-only) that the pattern was hardened against.
- Repair before consolidate, and never canonicalize an error. Three of the merges in this plan nominate a single source of truth for a fact that is currently stated several ways, and in two of those the nominated source is the wrong one: MISSION.md's C1-C10 enforcement column names five SQL objects that do not exist under those names, and WEBAUTHN-ROLLOUT.md's recovery procedure names a /auth/recovery route that does not exist. In every such case the sequence is grep-verify the object against the code, correct the intended source, then strip the duplicates, then add a check that the named objects resolve. A merge that runs in the other order deletes the accurate statements and leaves the false ones standing behind a link.
- Understatement is not the safe failure. Several of the worst defects run in the direction of hiding shipped, CI-proven capability: the architecture document says the Helm chart is deferred and bare-metal is not automated when both ship with CI jobs; SYSTEM-MAP names 7 of 14 CI jobs; OPERATIONS.md tells an operator the DR targets and operator MFA do not exist; the README undersells its own gate coverage by half. Correcting these adds no overclaim risk, because the direction of travel is toward what CI already proves, and it removes the worse outcome: an assessor who cross-checks ci.yml, finds twice the claimed coverage, and thereafter distrusts the honest claims about what Polaris does not do.

# Do not touch
- MISSION.md's freeze-line section (lines 10-77 including the amendment log, the six done-conditions, the (a)/(b)/(c) work classes, and the abandonment clause). Lines 67-76 state that any edit softening a condition or removing the abandonment clause breaks the constitutional contract and should be treated as a fork, and lines 19-21 state that no further amendments are pre-authorized. A documentation pass carries no such authorization. Three artifacts cite it by name: docs/THESIS.md:38, :67 and :127; polaris_checks/checks.py:204-211, whose comment records that it deliberately does not touch MISSION.md's freeze line; ROADMAP.md:10, which names it as the thing the 2026-08-31 trigger acted under. Record that the clauses fired; do not edit them.
- The heading strings `## Vocation` and `## The hard constraints (do not violate)` in MISSION.md. polaris_web/templates/landing.html:115 and :118 deep-link MISSION.md#vocation and MISSION.md#the-hard-constraints-do-not-violate from the public landing page. A reworded heading silently breaks both, and no link checker resolves anchors.
- README.md's `Enforced by` column in the C1-C10 table. It is the evidence for the README's own thesis that the guarantees live in the database rather than in application code, it is currently the most accurate such table in the repository (line 46's bidirectional CHECK and line 48's single-statement UPDATE ... RETURNING are both correct against polaris_sql/01_schema.sql:377 and polaris_checks/checks.py:1770), and it is what an assessor reads before deciding whether to open the SQL. Consolidating C1-C10 into MISSION.md must not delete it.
- The claim-to-pinning-check mapping inside docs/PRODUCTION-READINESS.md's closed bullets, and the caveats parenthesized inside them. Each closed bullet names the check that pins it; that is the only content in the body an assessor can execute, and it must survive the compression as a table. The parentheticals (HSM/KMS operator-gated, non-root Caddy edge as a follow-up, one postgres replica, offsite repo and schedule operator-supplied, Alertmanager backend operator-gated, pager URL operator-supplied) are the honest residue and must be harvested into the decision table, not swept out with the narrative. The `Status: NOT production-ready` top line and the closing rule stay in force.
- The literal string `gh attestation verify` in SECURITY.md (polaris_checks/checks.py:2724-2728 FAILs `provenance` without it), the literal `AgencyQuota` (checks.py:3674), `POLARIS_WEBAUTHN_ATTESTATION` in WEBAUTHN-ROLLOUT.md (checks.py:3533), `POLARIS_SECRETS_BACKEND` and `rotate-wrapping` in SECRETS.md section 8 (checks.py:3061), and DR.md's pointers at the drill and the ledger (checks.py:3778). These look like incidental prose and are check inputs.
- At least one schema-table count must remain in README.md and in docs/ARCHITECTURE-OVERVIEW.md. check_table_count_matches_doc (checks.py:1933) fails closed when a guarded document states no count. Stripping counts from the README diagram is safe only because the evidence table keeps one; removing both would turn a bloat cut into a red CI.
- `security.reject_cross_site` on /api/heartbeat and /api/quit. Those two routes are the guard's only application in the entire codebase, and CrossSiteGuardTests (test_app.py:6927-6952) is its only coverage. Deleting the routes to remove launcher scaffolding would silently retire a real security control; gate them and keep the tests running against a launcher-mode client.
- The DOM ids `atlas-globe-data` and `atlas-globe-detail` and the class `atlas-fullbleed`, despite polaris_web/static/atlas-globe.js being dead and deletable. polaris_web/test_app.py:286 and test_e2e_atlas.py:173 pin them; the file name and the id names are not the same thing.
- The three-command verification block under README's evidence table and the sentence that most CI jobs carry a comment naming the specific past failure they exist to prevent. That is the reader's only path from a stated claim to running the check themselves, and it is the strongest thing on the page.
- docs/operator/DR-DRILLS.md. It is a machine-appended ledger written by scripts/polaris-dr-drill.sh --record and committed monthly by .github/workflows/dr-drill.yml, which ci.yml deliberately ignores by path so the row does not spend a run. It is not a runbook, it must not be restyled or hand-edited, and it must not be counted among the runbooks. Its stated targets (RPO at most 300 s, RTO at most 14400 s) are the ground truth that DR.md, FAILOVER.md and docs/operator/README.md must all be corrected toward, not away from.
- The second `\i 09_grants.sql` at polaris_sql/00_load_all.sql:71. Line 70 states it re-applies grants to cover the new auth tables. It looks like a copy-paste duplicate and is the reason a naive renumber would silently drop grants on the 10_auth tables. It is also why prefix-order-equals-load-order is unreachable without splitting role creation from granting.
- The assessor-grade citations in docs/ARCHITECTURE-OVERVIEW.md's Further reading: meta/constraint-lattice.md, meta/structural-architecture.md, meta/redaction-proof.md and docs/design/threat-model.md. They read as internal-tree pointers and are the traceability for the C1-C10 completeness claim and the STRIDE model with its explicit ACCEPTED and DEFERRED rationale. Only DEVNOTES/style.md is genuinely internal in that list. Move them into the published set if desired; do not strip the references.
- CHANGELOG.md's historical entries. It is the audit of record; scripts/polaris-link-check.sh already exempts the root copy by path precisely because release history legitimately names files that have since moved. Rewriting past entries to match present filenames is itself a ledger violation, so a rename ships with a forward-looking mapping entry rather than a retroactive edit.
- docs/operator/README.md and docs/reference/README.md as sub-indexes. They are complete, well-written and carry reading orders for a first-time deploy, a compliance audit and a production incident. They look like duplication of docs/README.md and are the reason the hub structure works; flattening them into the top-level index would recreate the drift this row exists to close.
- The `[EXT: ...]` and `[OWNER-AUTHORIZED REWORK]` markers on ROADMAP rows, and the six mechanical done-conditions in MISSION.md's freeze line. The markers are the record of what is and is not within the project's own control, which is what keeps the phase map from reading as a promise; the done-conditions are six live, grep-checkable claims that are exactly the artifacts an external assessor wants.

---

# The critic's findings against the plan (2026-09-02)

Recorded so a later session sees what the rulings above answer.

## Surfaces the audit never opened

1. **`polaris_sql/README.md`**: states **"26 tables"**, cites `docs/paper/polaris_project_report.pdf` as the specification, and carries an unbroken wall of `M2-1…M2-11 / R10-2 / R11-3` attributions plus unstamped "54 foreign keys, 74 CHECK constraints". No row owns it, and it is not in the `check_table_count_matches_doc` guarded list, so the count sweep leaves the most-read schema document stating 26 against 29. Add it to the guarded list and to Ship 1.
2. **`polaris_zk/README.md`**: title is literally `# polaris_zk/: R10-1 / M2-1 · Plonky2 ZK-SNARK prover`, body carries "5th and final primitive in the Substrate-D arc", "Built v8.23". The identifier sweep is scoped to "operator and reference titles"; package READMEs get only the em-dash pass. Extend the sweep to `polaris_*/README.md`.
3. **`polaris_cli/README.md`**: `R10-2 / R11-3 / R10-1 / R11-5`, "v2 substrate", a stale "future backlog item" claim, and a link to `github.com/EgorKhaklin/polaris`.
4. **Package metadata URLs**: `polaris_cli/pyproject.toml:4,81` (`Homepage`) and `polaris_zk/Cargo.toml:9-10` (`repository`, `homepage`) point at `https://github.com/EgorKhaklin/polaris`, not `polaris-id`. These are the URLs PyPI and crates.io render; they are the integrator's first click and they are wrong. Not in any row.
5. **`docs/paper/README.md`**: carries the SCS-230 framing the plan says must live only in NOTICE, uses the "What this directory is NOT" pattern the plan deletes from `assets/README.md`, and links `../story/`, which Ship 7 deletes. Also `docs/paper/` never gets a row in the rebuilt `docs/README.md` hub.
6. **`docs/THESIS.md`**: a public front-door target (`landing.html` links it) and a check input (`check_thesis_terminus_honest`), carrying `**Author:** Egor Khaklin (VANTA)`. It gets no reader/job pass in any row, while P1.16 only asks to "settle on one maintainer identity" at `:5`.
7. **Operator documents with no owner in the plan**: `HARDENING.md`, `KEY-CEREMONY.md`, `RUNBOOKS.md`, `SLOS.md`, `FAILOVER.md`, `INSTALL.md` beyond `:125-136`, `PRIVACY.md` beyond `:85-94`, `PERFORMANCE-BASELINE.md`. Concretely: `RUNBOOKS.md:317,:328` put **R11-6** into incident text an on-call reads; `RUNBOOKS.md:8`, `SLOS.md:7`, `FAILOVER.md:8` carry the same `**Honest status (v9.175 / v9.123 / v9.126)**` blockquote the plan deletes only from DR.md; `PRIVACY.md:290` heads a section "Operational privacy posture in production (Arc B / v8.77)"; `HARDENING.md:184,:227` carry v9.189 in a section title and body.
8. **`deploy/observability/polaris-alerts.yml`**: the pager text. `description` at ~`:115` says "the **R11-6** percentage bound may already have engaged", and several summaries carry em-dashes. This is a first-class operator message surface and it is outside both the identifier sweep and the widened em-dash hook (which stops at `docs/`, `DEVNOTES/`, `meta/`, `site/`, package READMEs). Add `deploy/**` to both.
9. **`deploy/helm/polaris/templates/NOTES.txt`**, **`deploy/linux/*.service` `Documentation=` URLs**, **`deploy/linux/polaris.env.example`**: text printed to an operator at install time. `NOTES.txt` ends with a bare `docs/operator/KUBERNETES.md` (invisible to the link checker); the two `.service` files hard-link `docs/operator/OPERATIONS.md` and `LINUX-SERVER.md` as absolute GitHub URLs, which Ship 5 restructures.
10. **`.trivyignore`** and **`deploy/observability/grafana/dashboards/*.json`**: the CVE waiver register and the panel titles an assessor reads. Neither is triaged, and `.trivyignore:1` is stamped `(v9.138)` in the body.
11. **`CLAUDE.md`**: stays at the repository root, titled "agent runbook", while P1.14's stated goal is that nothing in the presentation reads as agent tooling; the plan simultaneously promotes it to the canonical source of the About sentence and adds it to a count check. No row decides whether an assessor should meet it at the root, or whether `docs/README.md`'s "Agent semantic memory → DEVNOTES/" framing survives it.
12. **`polaris_web/README.md`**: only `:100-101` and `:446` are touched, though it is the application package's front door.

## Templates the P1.17 enumeration misses

13. **`base.html`**: the plan strips only the SUBSTRATE menu (`:37-52`). The **USE CASES** menu below it renders eight visible `<span class="uc-id">UC-1 … UC-12</span>` kickers and the menu label itself is internal vocabulary. Also the `.uc-id` / `.nav-uc-*` class family.
14. **Nine page titles**: `uc1_issue.html:2` "UC-1: Issue New Token", `uc4_activate:2`, `uc5_bind:2`, `uc7_warrant:2`, `uc9_decide:2`, `uc9_queue:2` all render `UC-N:` in the browser tab and heading.
15. **Visible body text with identifiers**, none listed in the plan: `uc4_activate.html:20,:22` ("UC-4 requires…", "issue a RESERVE token first via UC-1"), `uc5_bind.html:18`, `uc9_queue.html:6` "(R11-2 / M2-7)", `tokens_list.html:32` button label "Issue New Token (UC-1)", `investigate_token.html:8` tooltip "(R11-5 anti-coercion primitive)" and `:18` "Per v9.18 anti-coercion vocation", `investigate_individual.html:14` same, `federation_viewer.html:6` "(R11-3 / M2-8 / v8.22)", `anchors_list.html:6` "(R10-2 / M2-2 / v8.21)".
16. **Missing decision**: the ban on UC-* does not say that the endpoint names (`url_for('uc1_issue')`), URL paths, and stored procedures (`uc1_issue_and_activate`) stay. Without that sentence a contributor renames routes and breaks `test_app.py`, the CLI and `05_procedures.sql`.

## Contradictions inside the plan

17. **QUICKSTART is deleted and edited in the same plan.** P1.13 deletes `docs/QUICKSTART.md`; P1.17 ordered change 1 says "Update `docs/QUICKSTART.md:153` and `:164`"; P1.14 replaces the SECURITY.md credentials with "a pointer to the quickstart". Resolve to README's quickstart and drop the QUICKSTART edits.
18. **The `docs/story/` inbound list is incomplete.** The plan names README, `docs/README.md:17`, `docs/reference/README.md`, CONVENTIONS §14. Also live: `CLAUDE.md:93`, `docs/reference/SYSTEM-MAP.md:9`, `:100`, `:152`, and `docs/paper/README.md`. `polaris-link-check.sh` goes red on the delete commit.
19. **The em-dash premise is false and the scope is understated.** The plan says the five hook-covered root files "are already clean at 0-1 occurrences": `MISSION.md` has **67** lines with em-dashes, `CLAUDE.md` 10, `CONTRIBUTING.md` 5. The sweep is not "roughly 150": `docs/` 536, `DEVNOTES/` 401, `polaris_sql/` 300, `scripts/` 213, `polaris_web/templates/` 151, `polaris_web/static/` 98, `deploy/` 38, `meta/` 28.
20. **The voice rule collides with `do_not_touch`.** `MISSION.md:1`, `:5`, `:10`, `:43-53` carry em-dashes inside the frozen region, and the frozen heading is `## Freeze line: definition of done (v9.27, amended once v9.29)`, which carries both an em-dash and two version numbers. State the freeze-region exemption explicitly. Also unassigned: `MISSION.md:225` "### Constraint lattice (v8.8)" and `:254` "### How the constraints are checked (v9.55)" are version-stamped headings and possible anchor targets.
21. **SLH-DSA is fixed on one surface only.** P1.15 deletes the site cell because no SLH-DSA signer exists, but `README.md:131-132` marks SLH-DSA-128s/256s with a PQ `✓` in the algorithm table and `README.md:136` states the full hedge argument ("if one family falls, the other stands"); `polaris_sql/04_data.sql:84-88,:132,:158,:338` seeds token T4 as "signed under SLH-DSA-128s"; `PQC-POSTURE.md` has no SLH-DSA gap row. Land one ruling (registry row versus implemented signer) across README, the seed comments, GLOSSARY and PQC-POSTURE, or the fix creates a fresh front-door contradiction.
22. **The "backed by a gate" sentence is narrowed on one surface only.** `README.md:145` carries the same claim as `site/index.html:224` under a v9.157 stamp. Narrow both until `check_stated_counts` exists, or say why they differ.
23. **8 versus 10 operator decisions.** `docs/PRODUCTION-READINESS.md:11-14` says ten decision gaps; the table at `:312-321` has eight rows. The plan asserts "eight" twice and deletes the sentence. Say that eight is measured from the table and forbid ten from reappearing on the site.
24. **29 versus 32 tables is left unresolved for the reader.** `^CREATE TABLE ` in `01_schema.sql` is 29; a migrated database has 32 (`OperatorWebauthnCredential`, `AuditAccessLog`, `OperatorSession`) plus `schema_version`. Telling the site to say "29 schema tables" leaves an assessor who runs `\dt` seeing 32. Adopt one phrasing ("29 in `01_schema.sql`, 32 after migrations") and note that the guarded regex `(\d+)\s+(?:schema )?tables` will FAIL on the literal "32 tables" inside any guarded document, so `DATA-MODEL.md` must state the delta without that token or the check needs a scoped pattern.
25. **DATA-MODEL new sections do not match the migration set.** The plan names three migration-added tables but four new sections, and `OperatorSession` gets neither. `AgencyQuota`, `ZkVerificationNonce` and `IndividualErasureEvent` exist in both `01_schema.sql` and a migration; say which is authoritative.
26. **`check_stated_counts` will measure the wrong CI number.** `ci.yml` has 17 top-level two-space keys (`push`, `pull_request`, `workflow_dispatch` under `on:` plus 14 under `jobs:`). The check must parse keys under `jobs:`, not indentation.

## Understated blast radius

27. **Renaming `docs/operator/SECURITY-CONTROLS.md`** touches `polaris_checks/checks.py:3534` and `:3675` (the very `SESSION_EVICTED` and `AgencyQuota` pins listed in `do_not_touch`), `polaris_checks/test_checks.py`, `polaris_web/app.py`, `security.py`, `test_app.py`, `polaris_mac_launch.sh`, `polaris_sql/13_substrate.sql`, `docs/README.md`, `SYSTEM-MAP.md`, `OPERATIONS.md`, four DEVNOTES files, and **`polaris_web/templates/landing.html`**, whose absolute `blob/main/docs/operator/SECURITY-CONTROLS.md` URL no link checker resolves. None of these appear in the P1.16 risk list.
28. **Moving or renaming `DEVNOTES/`** is not "roughly two dozen inbound links". `DEVNOTES/` is cited from about 80 files, including `polaris_sql/01_schema.sql` (15), `05_procedures.sql` (8), `13_substrate.sql` (4), `polaris_web/{security,pqc_signing,zk,app}.py`, seven templates, `polaris_zk/README.md`, `meta/README.md`, `docs/paper/README.md`, `GLOSSARY.md` (16), `DATA-MODEL.md` (14), and `.pre-commit-config.yaml`'s hook name and comment (`DEVNOTES/style.md`). Most are backticked bare paths the link checker cannot see, so the rot is silent.
29. **The `ai-*` rename caller list is incomplete**: also `polaris_web/requirements-dev.txt`, `DEVNOTES/known-gotchas.md`, `ROADMAP.md`, `docs/operator/WEBAUTHN-ROLLOUT.md`, and critically **`scripts/polaris-trace-drill.sh`** and **`scripts/polaris-chaos-test.sh`**, which invoke `polaris-test.sh`. `polaris-trace-drill.sh` runs in the `trace-drill` CI job, so a missed edit fails at runtime, not at lint.
30. **`docs/README.md` is worse than described.** It links 11 files and omits `PRODUCTION-READINESS.md`, `ARCHITECTURE-OVERVIEW.md`, `THESIS.md`, `CONVENTIONS.md`, `RED-TEAM-SCOPE.md`, `SEED_DATA.md`, `QUICKSTART.md`, 14 of the 17 operator documents, and `paper/` entirely. Before `check_docs_index_coverage` can pass on arrival, the plan must say how `DR-DRILLS.md` (a ledger that must not be restyled or counted as a runbook) and `docs/paper/` are represented in the graph.
31. **Scope arithmetic.** The target shape says "about thirty documents". The human-facing markdown set is 35 under `docs/`, 7 at the root, 5 package READMEs, `assets/README.md`, `deploy/observability/README.md`, 26 under `DEVNOTES/` and 5 under `meta/`: near 80, and P1.16 promotes nine `DEVNOTES` documents into the published set where the one-reader-one-job standard then applies to them too. Budget for it or state the exemption.

## Smaller decisions still open

32. **Latin.** The cross-cutting voice rule bans Latin; P1.15 keeps one of the two site occurrences (`:139` or `:285`) while P1.17 deletes it from `base.html:138`. Decide once.
33. **No `CODEOWNERS`** anywhere in the tree, and P1.14's community-file list does not add one or a documentation-issue route.
34. **The paper is cited as the specification** by `polaris_sql/README.md:3` and by `SYSTEM-MAP.md:152`'s academic reading order. P1.16 offers "release asset or CI rebuild" but no row repoints the two readers who treat the PDF as normative.