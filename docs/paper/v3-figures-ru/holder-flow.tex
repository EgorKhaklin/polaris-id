% v2-figures/holder-flow.tex: holder, relying party and issuer, with what each
% sees and what each is prevented from seeing. The one red edge is the
% documented limitation: the relying party sees a stable credential value.
\begin{tikzpicture}
  % columns
  \node[humanbox, minimum width=3.6cm, text width=3.3cm] (h) at (0,4.0) {Держатель\\[-1pt]{\mdseries\scriptsize кошелёк держит файл: значение токена, подпись, ключ эмитента; необязательно подшитое утверждение о состоянии, доказательство принадлежности, код предъявления}};
  \node[extbox, minimum width=3.6cm, text width=3.3cm] (rp) at (6.2,4.0) {Доверяющая сторона\\[-1pt]{\scriptsize запускает отделяемый верификатор или SDK автономно; либо обращается к эмитенту как к себе}};
  \node[layerbox, minimum width=3.6cm, text width=3.3cm] (iss) at (12.4,4.0) {Выдающий орган\\[-1pt]{\mdseries\scriptsize схема, подписывающий ключ, корни эпох, состояние}};
  % flows
  \draw[flow] (h.east) -- node[faint, above, text width=2.4cm] {предъявляет (QR, NFC, файл)} (rp.west);
  \draw[flow] (rp.east) -- node[faint, above, text width=2.6cm] {проверка состояния как организация, человек не назван} (iss.west);
  % what each decides / sees
  \node[softbox, anchor=north, text width=3.4cm] (hs) at (0,1.9) {\textbf{видит:} собственное удостоверение.\\\textbf{решает:} уровень раскрытия, под которым предъявляет.\\\textbf{не может:} повысить то, что запишет сервер (C6).};
  \node[softbox, anchor=north, text width=3.4cm] (rs) at (6.2,1.9) {\textbf{проверяет автономно:} подлинность; доверен ли эмитент; связано ли утверждение, свежо ли, ACTIVE ли; контекст.\\\textbf{узнаёт:} что действительное удостоверение существует; значение удостоверения.\\\textbf{не может:} добраться до записи человека; проверить полномочия свежее утверждения.};
  \node[softbox, anchor=north, text width=3.4cm] (is) at (12.4,1.9) {\textbf{записывает в режиме нулевого разглашения:} событие без идентификатора токена (C2).\\\textbf{записывает при сетевой проверке состояния:} ни строки <<кто кого проверил>>.\\\textbf{не может:} восстановить граф проверок с нулевым разглашением из собственной базы данных.};
  % the assertion fetched by possession, routed over the top of the three parties
  \draw[flowfaint] (iss.north) to[out=110, in=70, looseness=0.6] node[faint, above, text width=6.0cm, pos=0.5] {утверждение о состоянии забирается по владению; записи о том, кто спрашивал, нет} (h.north);
  % the limitation
  \draw[refuse, dash pattern=on 3pt off 2pt] (rs.south) -- (6.2,-3.2);
  \node[anchor=north, font=\scriptsize\bfseries\color{classmark}, text width=9.0cm, align=center] at (6.2,-3.3) {Заявленное ограничение: предъявление полного удостоверения несёт устойчивое \code{token\_value}, так что две доверяющие стороны способны соединить по нему свои журналы. Субъектом входного токена это значение больше не служит: начиная с \code{v9.353} он выводится для каждой доверяющей стороны отдельно. Несвязываемость действует со стороны эмитента и ограничена режимом нулевого разглашения; межверификаторная несвязываемость при обыкновенном предъявлении намеренно не реализована.};
\end{tikzpicture}
