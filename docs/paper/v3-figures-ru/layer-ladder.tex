% v3-figures/layer-ladder.tex: the metacognitive ladder, zooming from the person
% to the software of strangers. Each rung names what the layer knows and what it
% refuses to trust from the rung below.
\begin{tikzpicture}
  \foreach \i/\name/\knows/\refuses/\style in {
    0/{У0 \ Человек}/{существует за пределами программ}/{описанию человека, которое даёт программа}/humanbox,
    1/{У1 \ Ядро удостоверения}/{один действующий токен, одна подпись, один человек}/{любому состоянию, которого схема не способна представить}/corebox,
    2/{У2 \ Структурные ограничения}/{какие состояния вообще вправе существовать}/{коду приложения, обещающему вести себя прилично}/layerbox,
    3/{У3 \ Криптографические свидетельства}/{кто что подписал, проверяемо всяким}/{вердикту одной-единственной реализации}/layerbox,
    4/{У4 \ Проверка и состояние}/{подлинно и полномочно прямо сейчас}/{подлинности, выдаваемой за полномочия}/layerbox,
    5/{У5 \ Приватность и раскрытие}/{что верификатору дозволено узнать}/{утверждению клиента о собственном уровне раскрытия}/layerbox,
    6/{У6 \ Федерация}/{кому орган доверяет явно и в каком контексте}/{транзитивному доверию, центральному органу}/layerbox,
    7/{У7 \ Прозрачность}/{что история не переписана и не раздвоена}/{обзору журнала одним наблюдателем}/layerbox,
    8/{У8 \ Ведомственные службы}/{что обмен состоялся и был дозволен}/{хранению сообщения ради доказательства}/layerbox,
    9/{У9 \ Эксплуатация}/{что система переживает отказ и нагрузку}/{утверждению, не отработанному в учении}/layerbox,
    10/{У10 \ Познание системы}/{что система делает и как устроена власть}/{чему бы то ни было о человеке}/softbox,
    11/{У11 \ Общественная граница}/{чем Полярис отказывается становиться}/{деньгам, оценкам, обязательной двери}/softbox,
    12/{У12 \ Среда агентов и веба}/{какого рода деятель действует и под чьими полномочиями}/{устойчивому дескриптору при всяком предъявлении, кроме нулевого разглашения (остаток)}/corebox,
    13/{У13 \ Чужие программы}/{что нашли кошелёк постороннего, размещённый пакет проверок и опубликованный набор векторов}/{чему бы то ни было, что автор прогнал против собственного кода}/extbox} {
    \node[\style, anchor=west, minimum width=5.6cm, text width=5.3cm, minimum height=0.8cm] (r\i) at (0, -1.1*\i) {\name};
    \node[anchor=west, font=\scriptsize, text=navydark, text width=5.0cm, align=left] at (6.0, -1.1*\i) {\knows};
    \node[anchor=west, font=\scriptsize, text=classmark, text width=5.2cm, align=left] at (11.4, -1.1*\i) {\refuses};
  }
  \node[kicker, anchor=west] at (6.0, 1.0) {что слой знает};
  \node[kicker, anchor=west] at (11.4, 1.0) {чего он не принимает на веру};
\end{tikzpicture}
