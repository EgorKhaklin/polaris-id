% A-status.tex: every major capability with its status mark and the check that pins it.
\section{Capability status}
\label{app:status}

The table answers, for every major subsystem, whether it runs, what limits it, which invariant
check fails the build if it stops being true, and, where the mark says so, who outside the
repository has exercised it. The marks are those of the front matter. Every row was measured at
\code{1.0.0-rc.7}. Rows carrying \sWit{} point at a filled row of the repository's scoreboard; no row
moved from running to proposed since Version 2, and three moved from internal to externally
exercised.

\Needspace{14\baselineskip}
{\scriptsize
\setlength{\tabcolsep}{3.5pt}\renewcommand{\arraystretch}{1.12}
\rowcolors{2}{tabstripe}{white}
\begin{xltabular}{\textwidth}{>{\RaggedRight\arraybackslash\hspace{0pt}}p{1.46in} >{\RaggedRight\arraybackslash\hspace{0pt}}p{1.84in} >{\RaggedRight\arraybackslash\hspace{0pt}}X >{\RaggedRight\arraybackslash\hspace{0pt}}p{1.3in}}
\caption{Capability status at \code{1.0.0-rc.7}. A row carrying two marks runs today and still requires external validation for the stronger claim, or has been exercised from outside on one path and not on the others.}\label{tab:status}\\
\toprule
\rowcolor{tabheader}\thead{Capability} & \thead{Status} & \thead{Limit stated by the repository} & \thead{Pinned by} \\
\midrule
\endfirsthead
\toprule
\rowcolor{tabheader}\thead{Capability} & \thead{Status} & \thead{Limit stated by the repository} & \thead{Pinned by} \\
\midrule
\endhead
\midrule\multicolumn{4}{r}{\scriptsize\itshape continued on the next page}\\
\endfoot
\bottomrule
\endlastfoot
Schema constraints, C1--C10 & \sImplMark & The database owner is outside the threat model; three constraints carry a checked formal model & \code{check\_c1c10\_objects\_resolve}, the TLA+ drill \\
Append-only audit of record (fourteen named; thirty-one tables) & \sImplMark & Tested once per table that claims it; a reload once reached one record with neither its ids nor its rows & \code{check\_aor\_append\_only\_triggers}, \code{check\_append\_only\_tables\_are\_tested\_exhaustively}, \code{check\_seed\_restart\_resets\_dependent\_records} \\
Retention as data with a 365-day floor & \sImplMark & Archive and purge remain deliberate operator steps & \code{check\_retention\_engine} \\
Mutation drills over the verification layer (twelve) & \sImplMark & A shared misreading of a specification survives every one of them & \code{check\_constraint\_suite\_is\_mutation\_tested}, \code{check\_triggers\_are\_mutation\_tested}, \code{check\_procedure\_refusals\_are\_mutation\_tested}, \code{check\_sdk\_refusals\_are\_mutation\_tested}, \code{check\_zk\_witnesses\_are\_mutation\_tested} \\
ML-DSA-65 and ML-DSA-87 signing & \sImplMark & The default development and CI path signs a labelled placeholder; SLH-DSA registered, not wired & \code{check\_pqc\_signing\_wired}, \code{check\_real\_pqc\_default\_boot} \\
Two-witness verification, sampled at use & \sImplMark & Catches divergence, not a shared misreading & \code{check\_pqc\_second\_witness}, \code{check\_verify\_witness\_sampling} \\
ML-DSA-65 verification against a published corpus & \sWit & The primitive, not the protocol: 58 of 58 vectors under both witnesses & the \code{pqc-real} job \\
Key custody: file, PKCS\#11, KMS & \sImplMark\newline\sExt & Software token in CI, no hardware module; the KMS is a stand-in & \code{check\_key\_custody\_abstraction} \\
Key register and signed trust list & \sImplMark & Rotation of a live key file or slot is an operator ceremony & \code{check\_trust\_lifecycle} \\
Algorithm agility, population migration & \sImplMark\newline\sExt & A verifier cannot learn a deprecation; rollback and the mixed window unmeasured & \code{check\_algorithm\_agility}, \code{check\_quantum\_event\_readiness}, \code{check\_post\_quantum\_claims\_are\_agility} \\
Detached verifier, packaged, fuzzed and attacked & \sImplMark & Refuses to start without a declared crypto mode; needs the prover binary for the zero-knowledge path only & \code{check\_detached\_verifier}, \code{check\_verifier\_fuzz}, \code{check\_attacks\_run}, the product-boundary drill \\
Python and TypeScript SDKs, conformance suite (118 cases) & \sImplMark\newline\sExt & No external implementation of the native protocol; 44 verdict fields unconstrained by any case & \code{check\_conformance\_suite}, \code{check\_typescript\_sdk}, \code{check\_conformance\_asks\_the\_relying\_party\_question} \\
Frozen version 1, cross-version suite & \sImplMark & & \code{check\_protocol\_versioning} \\
OpenID4VP 1.0 + HAIP verifier & \sImplMark\newline\sWit & One wallet, one format, one path, ES256 throughout; \code{1.0.0rc7} OpenID Certified to the HAIP verifier profile (24 September 2026), that version and role only & \code{check\_oid4vp\_verifier\_boundary}, the OpenID4VP mutation drill, the scoreboard \\
Format bridges: ISO 18013-5 rendering, Verifiable Credential of a verdict & \sImplMark & Formats, not trust models; no standard mDL reader can verify the ML-DSA issuer signature & the mdoc and VC drills \\
Relying-party verify API & \sImplMark & Client-credentials only; mutual TLS deferred & \code{check\_relying\_party\_api} \\
Signed status assertion, offline authorization & \sImplMark & The revoked-but-valid window is the cost; its maximum rests partly on relying-party configuration & \code{check\_offline\_verification}, the status-freshness model \\
Epoch checkpoints, revocation feeds, status bundle, cache rules & \sImplMark\newline\sFut & Distribution at content-delivery scale is specified and drilled, not deployed & \code{check\_epoch\_revocation\_propagation}, \code{check\_federation\_status\_bundle}, \code{check\_status\_distribution} \\
Zero-knowledge membership proof, holder-side prover, scoped nullifier & \sImplMark\newline\sExt & No external audit; the issuer can compute any member's nullifier; the anonymity set has no floor above one & \code{check\_zk\_tree\_depth\_synced}, \code{check\_holder\_side\_prover}, \code{check\_scoped\_nullifier}, the witness differential \\
Issuer-side unlinkable verification records & \sImplMark & Scoped to zero-knowledge mode; adversary shaped so it cannot flatter the result & the C2 CHECK and model, the redaction suite, \code{check\_redaction\_adversary\_is\_not\_flattered} \\
Cross-verifier correlation, measured & \sImplMark & Bounded on the zero-knowledge path within the epoch's crowd against an equality adversary; exposed on plain and SD-JWT presentations & \code{check\_pairwise\_presentation}, the linkability lab \\
Wallet protocol, presentations, QR framing & \sImplMark & No native clients, no browser bridge & \code{check\_wallet\_presentation}, \code{check\_qr\_resource\_bounds} \\
Auth broker with registered policy & \sImplMark & No session product, no consent screen & \code{check\_auth\_broker}, \code{check\_broker\_policy\_bound} \\
Decisions about authorities, operators, quotas, ceilings and relying parties, recorded & \sImplMark & A shell and the tool are recorded on the same terms & the audit-of-record checks, \code{check\_every\_audit\_event\_has\_a\_writer} \\
Identity proofing, trusted referee, enrolment code & \sImplMark\newline\sExt & Evidence authenticity is the operator's word; no proofing has been performed with it & \code{check\_enrollment\_code}, \code{check\_trusted\_referee}, the proofing drill \\
Federation: roots, attestations, manifests & \sImplMark\newline\sExt & Attestations operator-recorded; two instances by one author & \code{check\_federation\_topology}, \code{check\_federation\_two\_instances} \\
Per-authority isolation of operators & \sImplMark\newline\sExt & A shared instance's owner, backup and schema are shared & \code{check\_route\_guards\_are\_derived\_not\_listed} \\
Cross-authority zero-knowledge & \sImplMark & Abstains without the binary; issuer bound by root & \code{check\_cross\_authority\_zk} \\
Transparency logs, monitor & \sImplMark & A monitor alone cannot see a split view & \code{check\_transparency\_log} \\
Witnesses, gossip, equivocation proof & \sImplMark\newline\sExt & Witnesses are drill processes; independence is a deployment & \code{check\_transparency\_gossip} \\
External-ledger publication & \sImplMark\newline\sFut & File backend only; chain drivers declared & \code{check\_transparency\_publication} \\
Public transparency report & \sImplMark & Cannot see an access that was never recorded & \code{check\_transparency\_program}, \code{check\_audited\_reads\_are\_logged} \\
Signed registry, exchange gateway and receipts, timestamp authority, document signing & \sImplMark & Bare algorithm names in the registry; notarial signing; anchor verification in verifier and SDKs & \code{check\_registry}, \code{check\_exchange\_gateway}, \code{check\_exchange\_receipt}, \code{check\_timestamp\_authority}, \code{check\_document\_signing}, \code{check\_ltv\_timestamp\_trust} \\
HA, replicas, partitioning, DR, chaos, rolling deploys & \sImplMark & Ephemeral CI hosts & \code{check\_ha\_automation} and its siblings \\
Second region as a standby cluster & \sImplMark & Asynchronous; the recovery point is not zero and is measured & \code{check\_multi\_region\_dr} \\
Capacity and cost models & \sImplMark\newline\sExp & Core counts extrapolated under a named assumption; nothing run at scale & \code{check\_capacity\_model} \\
Post-quantum TLS edge and application-to-pooler hop & \sImplMark & The pooler-to-database hop is classical, gated on a PostgreSQL release & \code{check\_edge\_pq\_kex}, \code{check\_internal\_kex\_measured} \\
Abuse controls, session and origin hardening, WebAuthn MFA, logs without PII & \sImplMark & Authenticators classical until hardware ships & \code{check\_abuse\_controls}, \code{check\_session\_origin\_hardening}, \code{check\_logs\_exclude\_pii} \\
Atlas & \sImplMark & Every aggregate graded against its data's growth & \code{check\_atlas\_console}, \code{check\_benchmark\_measures\_growth}, the C8 caps \\
Athena & \sImplMark & Read-only; no person node, which one check refuses & \code{check\_athena\_no\_person} \\
Card profile, software token, personalization, verifier device, duress timing & \sImplMark\newline\sFut & No card manufactured, no silicon certified & \code{check\_card\_profile}, \code{check\_card\_emulator}, \code{check\_verifier\_device}, \code{check\_duress\_on\_card} \\
Duress code, in the application and on the card & \sImplMark\newline\sExt & Duress-aware, not compulsion-resistant: resists the casual coercer only, and its record is indelible & \code{check\_duress\_alertable}, \code{check\_duress\_claims\_are\_aware} \\
Pilot wind-down; coexistence and sunset plan & \sImplMark\newline\sExt & No pilot has run; the sunset verdict rests on operator attestations & \code{check\_coexistence\_plan}, the wind-down drill \\
NIST 800-63 mapping; accessibility & \sImplMark\newline\sExt & Not a conformance claim; the automated third only & the assurance-mapping and accessibility drills \\
Formal models: C1, C2, C3, status freshness & \sImplMark & Models, not the code; each of three must fail a counterpart configuration & the TLA+ drill \\
Holder key bound to a credential & \sImplMark & Optional; the proof deliberately does not cover the presented code & \code{check\_holder\_key\_binding} \\
Agent delegation, revocable by the holder & \sImplMark & A plain binding still shows the service the credential & \code{check\_agent\_grant} \\
Four packages on public registries & \sImplMark & Distribution is not validation; no outside install reported & the publish workflow, the boundary drill \\
Metis (learning the system) & \sFut & Assessment required before code, and an outside need before the assessment & \\
Myrmex (stigmergic debugging) & \sFut & Proposed in this paper & \\
Blinded or one-time presentations; a native anonymous predicate & \sFut & The residue: a plain or interoperable credential shows a verifier stable material & \\
Hardware token, enrolment kit & \sFut & Roadmap rows, held & \\
A non-author operator, a pilot, certification of anything beyond the OpenID4VP verifier, external audit, independent witnesses & \sExt & External actors & the scoreboard \\
\end{xltabular}
}

\subsection*{The checks behind each layer card}

The layer cards in the body say what each layer is, trusts, refuses and sees. The invariant checks
and drills that fail the build when a card's claims stop being true are listed here, card by card,
so the body can be read without them.

{\small
\begin{description}
  \item[The credential core.] \code{check\_c1c10\_objects\_resolve}, the C3 property tests and the C3 model, the concurrency suite, \code{check\_card\_profile}, \code{check\_card\_emulator}, \code{check\_verifier\_device}, \code{check\_duress\_on\_card}, \code{check\_duress\_claims\_are\_aware}, the attack suite's forgery and tamper cases.
  \item[The walls.] \code{check\_aor\_append\_only\_triggers}, \code{check\_aor\_privilege\_boundary}, \code{check\_no\_fk\_cascade}, \code{check\_retention\_engine}, \code{check\_append\_only\_tables\_are\_tested\_exhaustively}, \code{check\_constraint\_suite\_is\_mutation\_tested}, \code{check\_triggers\_are\_mutation\_tested}, \code{check\_unique\_rules\_are\_tested\_exhaustively}, \code{check\_procedure\_refusals\_are\_mutation\_tested}, \code{check\_seed\_restart\_resets\_dependent\_records}, the SQL self-tests.
  \item[Cryptographic evidence.] \code{check\_pqc\_signing\_wired}, \code{check\_pqc\_second\_witness}, \code{check\_verify\_witness\_sampling}, \code{check\_real\_pqc\_default\_boot}, \code{check\_trust\_lifecycle}, \code{check\_algorithm\_agility}, \code{check\_quantum\_event\_readiness}, \code{check\_zk\_witnesses\_are\_mutation\_tested}, \code{check\_post\_quantum\_claims\_are\_agility}, the canonical-equivalence oracle, the attack suite.
  \item[Independent verification.] \code{check\_detached\_verifier}, \code{check\_conformance\_suite}, \code{check\_typescript\_sdk}, \code{check\_verifier\_fuzz}, \code{check\_attacks\_run}, \code{check\_protocol\_versioning}, \code{check\_sdk\_refusals\_are\_mutation\_tested}, \code{check\_conformance\_asks\_the\_relying\_party\_question}, \code{check\_oid4vp\_verifier\_boundary}, \code{check\_documented\_verifier\_commands\_run}.
  \item[Verification and status.] \code{check\_relying\_party\_api}, \code{check\_offline\_verification}, \code{check\_epoch\_revocation\_propagation}, \code{check\_federation\_status\_bundle}, \code{check\_status\_distribution}, the status-freshness model, the verify-load drills under failover.
  \item[The privacy boundary.] The C2 CHECK and the C2 model, \code{check\_c6\_atlas\_redacts\_zk\_location}, the redaction property suite, \code{check\_redaction\_adversary\_is\_not\_flattered}, \code{check\_pairwise\_presentation}, \code{check\_scoped\_nullifier}, \code{check\_audited\_reads\_are\_logged}, the ZK second-witness differential, \code{check\_athena\_no\_person}.
  \item[Use.] \code{check\_holder\_wallet}, \code{check\_wallet\_presentation}, \code{check\_holder\_verifier\_flow}, \code{check\_auth\_broker}, \code{check\_broker\_policy\_bound}, \code{check\_relying\_party\_api}, \code{check\_qr\_resource\_bounds}, \code{check\_documented\_verifier\_commands\_run}.
  \item[Federation.] \code{check\_federation\_topology}, \code{check\_inter\_authority\_protocol}, \code{check\_epoch\_revocation\_propagation}, \code{check\_cross\_authority\_zk}, \code{check\_federation\_two\_instances}, \code{check\_exchange\_trust\_directional}, \code{check\_route\_guards\_are\_derived\_not\_listed}.
  \item[Transparency.] \code{check\_transparency\_log}, \code{check\_transparency\_gossip}, \code{check\_transparency\_publication}, \code{check\_timestamp\_transparency}, \code{check\_transparency\_program}, \code{check\_audited\_reads\_are\_logged}, the canonical-equivalence oracle for the signed head.
  \item[Institutions.] \code{check\_registry}, \code{check\_exchange\_gateway}, \code{check\_exchange\_receipt}, \code{check\_timestamp\_authority}, \code{check\_document\_signing}, \code{check\_ltv\_timestamp\_trust}, \code{check\_broker\_policy\_bound}.
  \item[Operations.] \code{check\_ha\_automation}, \code{check\_multi\_region\_dr}, \code{check\_read\_replica\_routing}, \code{check\_event\_table\_partitioning}, \code{check\_dr\_drill\_scheduled}, \code{check\_chaos\_program}, \code{check\_zero\_downtime\_deploy}, \code{check\_capacity\_model}, \code{check\_abuse\_controls}, \code{check\_every\_audit\_event\_has\_a\_writer}, \code{check\_logs\_exclude\_pii}, \code{check\_container\_hardening}, \code{check\_cve\_scanning}, \code{check\_drills\_count\_their\_cases}, \code{check\_coexistence\_plan}.
  \item[System cognition.] \code{check\_athena\_no\_person}, \code{check\_athena\_read\_only}, \code{check\_athena\_functions\_bounded}, \code{check\_athena\_non\_sovereign}, \code{check\_athena\_rule\_enforcement\_resolves}, \code{check\_atlas\_console}, \code{check\_benchmark\_measures\_growth}, the C8 caps.
  \item[The assurance layer.] Its own mutation drills, including one over the check layer and one over the
conformance runner; the detection tests of every check.
\end{description}
}

\subsection*{Operations: the drills behind Section~\ref{sec:operations}}

\begin{description}
  \item[High availability.] The database runs under Patroni, which elects the primary, with its leader lease
    held in a three-member etcd consensus store, and role-based routing. Under a live write stream the drill loses the leader (the replica
    promoted after the 20-second lease, in-flight queries failing fast and retried, none lost), cuts
    the leader off from the lease store (it stood down in 7 seconds), performs a planned switchover
    (3.3 seconds, no insert failed), and crashes an etcd member (a 0.3-second stall). The Kubernetes
    profile runs the same members with the cluster's API as the lease store and deletes the leader
    pod. \sImpl
  \item[A second region.] The HA profile survives a node dying; it does not survive the region, and
    the tempting fix, a third member in region B, is wrong three ways at once, because it puts the
    wide-area network inside the lease store's quorum, on every commit, and inside region A's blast
    radius. So region B is a standby cluster: its own lease store, a different
    scope, streaming asynchronously through region A's router so a failover inside A does not break
    replication, electing no primary of its own so two regions can never both accept writes. The
    recovery point is therefore not zero, and it is measured rather than asserted: the drill cuts
    region A the way a region goes dark, promotes B under a live write stream, and reports the time
    to serve (5 seconds) and the rows acknowledged in A and absent from B (zero at the drill's
    rate), against ceilings of 90 seconds and 50 rows. It asserts two things that matter more than
    either number: B holds no row A never acknowledged, and what crossed is a contiguous prefix,
    because a standby holding rows one to forty and forty-five to sixty has skipped rather than
    lagged. A returned region A is rebuilt as a standby and never brought back as a primary. \sImpl
  \item[Read replicas.] Read-only surfaces route to a streaming replica under an explicit
    staleness contract, at most ten seconds behind by default, falling back to the primary beyond
    it, with every routed response naming its source and lag. Correctness-critical reads stay on
    the primary, and the authorization half of a verification verdict is pinned there
    (Section~\ref{sec:authorization}). \sImpl
  \item[Partitioning.] The four append-only event tables are monthly range-partitioned, and a
    drill proves that the append-only trigger holds across a partition, an attach and a detach,
    because the archives' carve-out must not become a rewrite path. \sImpl
  \item[Disaster recovery.] Backups are encrypted and a restore fails closed without the key.
    A monthly drill kills the primary, restores it from the archive and commits the measured numbers
    against the targets of 300 seconds of data loss and four hours to recover: the last recorded
    rows show 36 to 42 seconds of loss and under five seconds to service, on the drill's ephemeral
    host, not production hardware. \sImpl
  \item[Chaos.] A weekly drill boots the blue-green stack under traffic, kills one colour with zero
    dropped requests, stops both until the outage page reaches a real alert manager and webhook,
    kills Redis and PostgreSQL, partitions the pooler, and commits every recovery time against a
    ceiling. Findings become checks. \sImpl
  \item[Rolling deploys.] A deploy under traffic drops zero requests; edge and database recreation
    are window operations measured on every push against ceilings (0.3 seconds for the edge on a
    single host, 0.6 seconds for a database crash), and the readiness ledger carries the remaining
    edge half openly. \sImpl
\end{description}

\subsection*{Performance numbers}

{\footnotesize
\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.12}
\rowcolors{2}{tabstripe}{white}
\begin{xltabular}{\textwidth}{>{\RaggedRight\arraybackslash\hspace{0pt}}X >{\RaggedRight\arraybackslash\hspace{0pt}}p{1.8in} >{\RaggedRight\arraybackslash\hspace{0pt}}p{0.8in} >{\RaggedRight\arraybackslash\hspace{0pt}}p{1.15in}}
\caption{Performance numbers at the versions that measured them. Every row is single-node on the reference laptop unless marked as a simulation or a projection. The event-ingestion rate and the cryptographic verification rate differ by about 35 times, a gap the repository's own history once hid (Section~\ref{sec:senses}). Rows the repository has not re-measured since Version 2 carry their original stamp.}\label{tab:perf}\\
\toprule
\rowcolor{tabheader}\thead{Quantity} & \thead{Number} & \thead{Kind} & \thead{Source} \\
\midrule
\endfirsthead
\toprule
\rowcolor{tabheader}\thead{Quantity} & \thead{Number} & \thead{Kind} & \thead{Source} \\
\midrule
\endhead
\midrule\multicolumn{4}{r}{\itshape continued on the next page}\\
\endfoot
\bottomrule
\endlastfoot
ML-DSA-65 sign, one core & about 2,080 per second & measured & dyno, \code{1.0.0-rc.7} \\
ML-DSA-65 verify, single witness, one core & about 7,950 per second & measured & dyno, \code{1.0.0-rc.7} \\
ML-DSA-65 verify, two witnesses, one core & about 740 per second & measured & dyno, \code{1.0.0-rc.7} \\
Membership proof, depth 14 & about 25 ms to prove, 12 ms to verify, 76 KB & measured & dyno, \code{1.0.0-rc.7}; the soundness ledger \\
Closing an epoch at depth 24, one million members & 2.1 s, tens of megabytes (was 10.8 s and 2.9 GB for a thousand) & measured & epoch cadence, \code{v9.357} \\
Re-signing a population under ML-DSA-87, one runner & about 345 credentials per second, 91 percent of it signing & measured & quantum-event drill, \code{v9.365} \\
Issuance through the application & 40 per second sustained, p95 28 ms & measured & baseline, \code{v9.191} \\
Verification event through the application & 80 per second sustained, p95 18.5 ms & measured & baseline, \code{v9.191} \\
Verification latency on the two-member HA topology under failover & a p50 at about 2 requests per second; p95 and p99 withheld, the sample cannot carry them & measured & failover drill, \code{v9.387} \\
Enrolment with real signing, bulk pipeline & about 372 tokens per second & simulated, 1:10,000 & national simulation, \code{v9.257} \\
Verification-event ingestion (audit writes, no signature check) & about 25,970 per second & simulated, 1:10,000 & national simulation \\
Atlas aggregates, 8,010 to 80,010 events & every bounded aggregate grew slower than its data (0.72x to 0.89x of linear) & measured & benchmark, \code{v9.402} \\
Single-witness verification across an eight-core node & about 62,800 per second & projection, fan-out assumed & benchmark arithmetic \\
\end{xltabular}
}
