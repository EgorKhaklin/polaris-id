# polaris_web/requirements-patroni.txt — the HA supervisor inside the database
# image (v9.243, roadmap P2.7). Installed by Dockerfile.postgres; a container
# that runs the default `postgres` command never imports any of it. Every
# transitive package is pinned so the image is reproducible; Dependabot's pip
# ecosystem bumps them like the app's requirements.
patroni[etcd3]==4.1.5
psycopg[binary]==3.3.6
psycopg-binary==3.3.5
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
dnspython==2.8.0
idna==3.19
prettytable==3.18.0
psutil==7.2.2
py-consul==1.7.1
python-dateutil==2.9.0.post0
python-etcd==0.4.5
PyYAML==6.0.3
requests==2.34.2
six==1.17.0
urllib3==2.7.0
wcwidth==0.8.3
# Held below 1.5 BY PATRONI, not by preference: patroni 4.1.5 declares
# ydiff!=1.4.0,!=1.4.1,<1.5,>=1.2.0, so 1.5 makes the image unbuildable
# ("Cannot install patroni and ydiff==1.5"). Dependabot proposed 1.5 on
# 2026-09-16, it was applied, and every container job went red until it was
# reverted; dependabot.yml now ignores it. Raise this only when patroni's
# own upper bound moves.
ydiff==1.4.2
